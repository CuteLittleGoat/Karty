# Analiza uwag użytkownika z pliku `Analizy/apka pop.docx` (2026-09-03)

## 1. Prompt użytkownika (zachowany dla kontekstu)

> Zapoznaj się z repo "Karty".
>
> W Analizy/apka pop.docx wrzuciłem uwagi użytkownika wraz ze screenami.
> Utwórz nowy plik Analizy/Uwagi_2026-09-03.md i zapisz tam pełną analizę dotyczącą opisanych w Analizy/apka pop.docx problemów.
>
> Nie zmieniaj kodu aplikacji. Możesz zaktualizować dokumentację w folderach:
> Main/docs
> Second/docs
>
> jeżeli przy przeglądzie aplikacji zauważysz gdzieś błąd.
> Poza tym nie zmieniaj kodu aplikacji.
>
> Na końcu analizy zrób punkt, w którym prostym językiem (dla kogoś, kto zupełnie nie zna się na programowaniu) wyjaśnisz czy to da się naprawić, jakie mogą być nieoczekiwane konsekwencje (typu, Zmiana X spowoduje, że Y...) i pytania doprecyzowujące. Pytania też mają być prostym językiem.
> Jeżeli w trakcie przeglądu aplikacji zauważysz jakieś błędy i problemy to też je dopisz do pliku z analizą.

---

## 2. Materiał źródłowy — treść `apka pop.docx`

Dokument zawiera 7 obrazów (`image1`–`image7`, z czego `image3` jest osadzony w relacjach pliku, ale nie występuje w treści dokumentu — jest to zrzut zakładki „Statystyki” z zaznaczonymi przyciskami Waga1/Waga2/Waga3, powiązany z uwagą nr 3).

Odtworzona treść w kolejności występowania:

**Profil admina:**

1. `-zakładka "gry admina" nie działa przycisk w kolumnie "czy zamknięta" (nie zależnie czy jest przycisk wciśnięty, statystyki zaciągają się do głównej tabeli)`
   → *screen `image1`*: zakładka **GRY ADMINA**, czerwoną pętlą zaznaczona kolumna `CzyZamknięta` (część gier zaznaczona, część nie), pod tabelą widoczne „Podsumowanie gry GRUBY”.
2. `-zakłada "gry użytkowników" wprowadzić na końcu (za przyciskiem usuń) dodatkową kolumnę o nazwie "importuj". Będzie to przycisk, przy każdej utworzonej grze. wciśnięty ma za zadanie skopiować utworzoną grę przez gracza i przekierować ją do zakładki "gry admina". (ze wszystkimi danymi)`
   → *screen `image2`*: zakładka **GRY UŻYTKOWNIKÓW**, czerwony „X” na prawo od kolumny `USUŃ` (miejsce na nową kolumnę).
3. `- zakładka "statystyki" nie działają przyciski w kolumnach "waga". Nie zmieniają się wartości.`
   → *screen `image3`*: zakładka **STATYSTYKI**, zaznaczone przyciski `WAGA1`, `WAGA2`, `WAGA3`.
4. `-Zakładka „gracze” jeśli dodam gracza o nazwie anonim to podczas tworzenia gry w szczegółach występuje gracz o takim nicku. Tak samo jak w późniejszym podsumowaniu gry oraz statystykach. I jeśli w zakładce „gracze” zmienię nazwę lub coś dopiszę u tego samego gracza, to w szczegółach gry nazwa się zmienia. Jest ok. Ale w podsumowaniu gry jak i w statystykach (Adolfa) już nie ma. Pozostaje dalej anonim`
   → *screeny*: `image4` (modal „Szczegóły”, wiersz 3 = `anonim`), `image6` (ten sam modal po zmianie nazwy — wiersz 3 = `Adolf`), `image5` (Podsumowanie gry `Gra 1` oraz tabela Statystyki — w obu nadal `anonim`).

**Profil admina oraz użytkownika:**

5. `-zakładka "statystki" przycisk "eksportuj" rozszerzyć nazwy kolumn w excelu, wyśrodkować nazwy kolumn oraz wartości dla lepszej widoczności, użyć separatora 1000 dla podanych wartości`
6. `- zakładka "gry użytkowników" przycisk „statusy” wprowadzić numeracje przy potwierdzeniach graczy. Chodzi o to, że musze widzieć w jakiej kolejności gracze potwierdzili swój udział w grze. Najlepiej chyba dodać nową kolumnę przed kolumna „gracz”. Jeśli pierwszy potwierdził goździk ma jedynkę, żbik potwierdził jako 2 więc jest przypisany do dwójki, a trzeci misiek. Przydatne w momencie jak mam grę na osiem osób a chętnych 12. (kto pierwszy ten lepszy) muszę widzieć kto w jakiej kolejności potwierdził swój udział. Wprowadzić taką funkcje również w zakładce „gry do potwierdzenia” profil admina oraz to później…`
   → *screen `image7`*: modal **Status potwierdzeń** z ręcznie dopisanymi cyframi 1, 2, 3 przy Goździk / Żbik / Misiek.

---

## 3. Zakres i metoda analizy

- Wszystkie zgłoszenia dotyczą **wyłącznie modułu `Main`** (`Main/index.html`, `Main/app.js`).
- Moduł `Second` („Tournament of Poker”) ma zupełnie inny zestaw zakładek (Lista graczy / Losowanie stołów / Wpłaty / Podział puli / Faza grupowa / Półfinał / Finał / Wypłaty / Czat) i **żaden z opisanych problemów go nie dotyczy** — potwierdzone skanem `Second/app.js` (brak `isClosed`, brak wag, brak `admin_games_stats`, brak eksportu XLSX, brak `confirmations`).
- Kod nie był modyfikowany. Wykonano: przegląd kodu, weryfikację składni (`node --check` — OK dla obu modułów) oraz statyczny skan zakresów zmiennych, który potwierdził jeden realny błąd referencji (patrz pkt 6).

---

## 4. Zgłoszenie 1 — „CzyZamknięta” w zakładce „Gry admina” nie wpływa na statystyki

### Stan faktyczny
Sam checkbox **działa poprawnie** w sensie technicznym: zapisuje i odczytuje pole `isClosed` w dokumencie gry.

- `Main/app.js:7967-7974` (zakładka „Gry admina”):
  ```js
  const closedInput = document.createElement("input");
  closedInput.type = "checkbox";
  closedInput.checked = Boolean(game.isClosed);
  closedInput.addEventListener("change", () => {
    void db.collection(gamesCollectionName).doc(game.id).update({ isClosed: closedInput.checked });
  });
  ```
- Analogicznie `Main/app.js:3293-3302` dla zakładki „Gry użytkowników”.

### Przyczyna zgłoszenia
Pole `isClosed` **nie jest w ogóle używane przy liczeniu statystyk ani podsumowań**. W całym `Main/app.js` `isClosed` występuje wyłącznie w dwóch filtrach:

| Miejsce | Kod | Efekt |
|---|---|---|
| `Main/app.js:1339` | `.filter((game) => !Boolean(game.isClosed))` | ukrywa zamknięte gry w „Gry do potwierdzenia” (admin i gracz) |
| `Main/app.js:1935` | `.filter((game) => !game.isClosed && isTodayOrFutureGame(game))` | ukrywa zamknięte gry w „Najbliższa gra” |

Natomiast selektory gier używane przez statystyki i podsumowania **nie filtrują po `isClosed`**:

- `Main/app.js:7642-7649` (`initAdminGames.getGamesForSelectedYear`) — filtr wyłącznie po roku,
- `Main/app.js:6912-6919` (`initStatisticsView.getGamesForSelectedYear`) — filtr wyłącznie po roku,
- `Main/app.js:2953-2960` (`initUserGamesManager.getGamesForSelectedYear`) — filtr po dostępie i roku.

W konsekwencji funkcje `getPlayersStatistics()` (`Main/app.js:7774` dla „Gry admina” i `Main/app.js:6949` dla „Statystyki”) oraz `renderSummaries()` liczą **wszystkie gry z danego roku**, niezależnie od stanu checkboxa. Dokładnie to opisuje użytkownik: „nie zależnie czy jest przycisk wciśnięty, statystyki zaciągają się do głównej tabeli”.

### Klasyfikacja
**To nie jest błąd implementacji, tylko brak zdefiniowanej reguły biznesowej.** Dokumentacja (`Main/docs/README.md`, sekcja 10.3 i „Kolumna CzyZamknięta”) opisuje ten checkbox jako czystą flagę „zamknięta / otwarta”, bez wpływu na statystyki — co jest zgodne z kodem, ale rozmija się z oczekiwaniem użytkownika.

### Co trzeba rozstrzygnąć przed naprawą
Nie da się poprawnie zaimplementować bez decyzji, którą semantykę przyjąć. Możliwe warianty:

- **A. „Zamknięta = rozliczona, liczy się do statystyk”** — do statystyk wchodzą tylko gry z `isClosed === true`. Wtedy w danych z `image1` do statystyk weszłoby 6 z 9 gier, a nie 9.
- **B. „Zamknięta = zarchiwizowana, nie liczy się”** — odwrotnie, gry zaznaczone są pomijane. Wtedy weszłyby 3 gry.
- **C. Checkbox zostaje wyłącznie flagą organizacyjną** (stan obecny), a do sterowania statystykami dodaje się osobny mechanizm.

Wariant A jest spójny z tym, że gra „zamknięta” znika z „Gry do potwierdzenia” i „Najbliższa gra” (czyli jest już rozegrana), ale wariant B jest spójny z potocznym rozumieniem słowa „zamknięta/archiwalna”. Bez odpowiedzi użytkownika każda implementacja jest zgadywaniem — pytanie zadane w pkt 13.

### Miejsca do zmiany (po decyzji)
Jeżeli zapadnie decyzja A lub B, zmiana musi objąć **spójnie wszystkie cztery miejsca**, inaczej liczby w zakładkach „Gry admina” i „Statystyki” się rozjadą:
1. `initAdminGames.getGamesForSelectedYear` (`Main/app.js:7642`) — wpływa na `Podsumowania`, `Statystyki` i `Ranking` w zakładce „Gry admina”,
2. `initStatisticsView.getGamesForSelectedYear` (`Main/app.js:6912`) — zakładka „Statystyki” (admin i gracz),
3. `synchronizeYearsFromGames` / lista lat — rok, w którym wszystkie gry są odfiltrowane, powinien zniknąć z panelu „Lata” albo pokazywać zero,
4. `Podsumowania` pod tabelą gier — czy podsumowanie odfiltrowanej gry ma nadal być widoczne (rekomendacja: tak, bo to widok pojedynczej gry, a nie agregat).

---

## 5. Zgłoszenie 2 — przycisk „Importuj” w „Gry użytkowników” (nowa funkcja)

### Stan faktyczny
Tabela w zakładce „Gry użytkowników” (`Main/index.html:264-275`) ma kolumny: `Rodzaj Gry`, `Data`, `Nazwa`, `CzyZamknięta`, `IlośćPotwierdzonych`, oraz pustą kolumnę z przyciskiem `Usuń`. Wiersz jest budowany w `Main/app.js:3338`:
```js
row.append(gameTypeCell, dateCell, nameCell, closedCell, confirmationsCell, deleteCell);
```

### Dlaczego ta funkcja ma sens (ważne odkrycie)
Gry użytkowników i gry admina to **dwie różne kolekcje Firestore**:
- gry admina → `Tables` (`GAMES_COLLECTION`, `Main/app.js:104`, potwierdzone w `config/firebase-config.js`: `gamesCollection: "Tables"`),
- gry użytkowników → `UserGames` (`USER_GAMES_COLLECTION`, `Main/app.js:105`).

Zakładka „Statystyki” agreguje **wyłącznie kolekcję `Tables`** (`initStatisticsView` używa `getGamesCollectionName()`, `Main/app.js:6850`). Zakładka „Gry użytkowników” w ogóle nie ma sekcji statystyk — tylko tabelę i podsumowania per gra.

**Wniosek:** obecnie gry stworzone przez graczy nigdy nie wchodzą do statystyk ani do rankingu. Przycisk „Importuj” jest więc jedyną sensowną drogą, żeby taka gra się w nich policzyła. To realna luka funkcjonalna, nie kaprys.

### Co dokładnie trzeba skopiować
Dokument gry (`UserGames/{id}`) — pola tworzone w `Main/app.js:3576-3583` i edytowane w tabeli:
`gameType`, `gameDate`, `name`, `isClosed`, `preGameNotes`, `postGameNotes`, `createdAt`, `createdByPlayerId`, `createdByPlayerName`, `createdByPlayerPin`.

Podkolekcja `rows` (`GAME_DETAILS_COLLECTION = "rows"`) — pola z `Main/app.js:3608-3616` i `Main/app.js:2842-2856`:
`playerId`, `playerName`, `entryFee`, `rebuy`, `rebuys[]`, `rebuyIndexes[]`, `rebuyNextIndex`, `payout`, `points`, `championship`, `createdAt`.

Podkolekcja `confirmations` — `playerId`, `playerName`, `confirmed`, `updatedBy`, `updatedAt`.

Reguły Firestore (`Analizy/Wazne_Rules.txt`) pozwalają na zapis do `Tables/{tableId}` oraz jej podkolekcji (`allow read, write: if true`), więc kopiowanie po stronie klienta jest technicznie możliwe. Liczba operacji (1 gra + N wierszy + M potwierdzeń) mieści się w limicie 500 operacji na batch.

### Ryzyka i pułapki tej funkcji
1. **Podwójne liczenie / duplikaty.** Kliknięcie „Importuj” dwa razy utworzy dwie kopie gry w `Tables`, a więc podwoi statystyki graczy. Konieczny znacznik (np. `importedFromUserGameId`) i sprawdzenie przed zapisem, albo blokada przycisku po imporcie.
2. **Kopia vs. przeniesienie.** Jeżeli gra zostaje też w `UserGames`, to po imporcie istnieją dwa niezależne rekordy: późniejsza korekta wypłat przez gracza **nie** zaktualizuje kopii w statystykach.
3. **Utrata obiegu potwierdzeń.** Potwierdzenia obecności działają tylko dla `UserGames` — zarówno widok gracza (`Main/app.js:2381`), jak i widok admina (`Main/app.js:3771`) pobierają gry tylko z `UserGames`; `nextGamesState.adminGames` jest na stałe pustą tablicą (`Main/app.js:1908`, `Main/app.js:2020`). Gra przeniesiona do `Tables` **znika z „Gry do potwierdzenia” i z „Najbliższa gra”** i nikt nie może już w niej potwierdzić udziału.
4. **Wpływ na `admin_games_stats`.** Import gry dodaje do rocznych statystyk nowych graczy, którzy dostaną domyślną wagę `"1"` (`getDefaultStatsManualFieldValue`, `Main/app.js:167-173`), podczas gdy pozostali mogą mieć np. `5`. Ranking (`Wynik`) przeliczy się dla całego roku.
5. **Zmiana roku.** Jeżeli gra ma datę z innego roku niż aktualnie wybrany, po imporcie „zniknie” z widoku — trafi do panelu „Lata” pod inny rok. Warto po imporcie przełączyć rok albo pokazać komunikat.

---

## 6. Zgłoszenie 3 — przyciski „Waga1…Waga6” w zakładce „Statystyki” nie działają — **POTWIERDZONY BŁĄD**

### Diagnoza
To jest twardy błąd w kodzie: **`ReferenceError`**.

Handler kliknięcia przycisku wagi w zakładce „Statystyki” (`Main/app.js:7326-7357`, wewnątrz funkcji `initStatisticsView`, która zajmuje linie **6819–7396**) wywołuje pomocniczą funkcję `ensureYearMapEntry`:

```js
// Main/app.js:7346
const playerEntry = ensureYearMapEntry(yearMap, row.statsKey, row.playerName, row.playerId);
```

Ale `ensureYearMapEntry` jest zadeklarowana jako `const` **wewnątrz zupełnie innej funkcji** — `initAdminGames` (linie **7427–8649**):

```js
// Main/app.js:7730
const ensureYearMapEntry = (yearMap, playerKey, playerName, playerId = "") => { ... };
```

Nie jest to funkcja globalna. W `Main/app.js` identyfikator `ensureYearMapEntry` występuje tylko 4 razy: definicja (7730) i trzy wywołania — 7759 i 8200 (oba wewnątrz `initAdminGames`, poprawne) oraz **7346 (wewnątrz `initStatisticsView`, poza zasięgiem)**.

### Skutek dla użytkownika
1. Użytkownik klika np. `WAGA1` w zakładce „Statystyki”.
2. Pojawia się okno `Podaj wartość liczbową dla kolumny Waga1.` — bo `window.prompt` jest wołany **przed** feralną linią (`Main/app.js:7330`).
3. Użytkownik wpisuje wartość i zatwierdza.
4. Pętla `statistics.playerRows.forEach(...)` wyrzuca `ReferenceError: ensureYearMapEntry is not defined` przy pierwszym graczu.
5. Wyjątek przerywa handler — **nie wykonuje się ani `renderStats()`, ani `persistYearConfig()`**. Nic się nie zmienia, żaden komunikat nie pojawia się w UI (błąd trafia tylko do konsoli przeglądarki).

Dokładnie to opisuje użytkownik: „nie działają przyciski w kolumnach waga. Nie zmieniają się wartości.”

Dotyczy to **wszystkich sześciu** przycisków (`Waga1`–`Waga6`), a nie tylko trzech zaznaczonych na screenie.

### Dlaczego w „Gry admina” te same przyciski działają
Bliźniacza obsługa w `initAdminGames` (`Main/app.js:8442-8459`) wywołuje `applyBulkWeightValue` (`Main/app.js:7741-7766`), która korzysta z `ensureYearMapEntry` **będącej w tym samym zasięgu**. Ta ścieżka działa poprawnie i zapisuje do `admin_games_stats/{rok}`.

Skutek uboczny: ponieważ obie zakładki zapisują do **tej samej kolekcji** `admin_games_stats`, użytkownik ma obejście — ustawienie wagi w „Gry admina” jest natychmiast widoczne w „Statystyki”. Prawdopodobnie dlatego problem nie zablokował pracy całkowicie (na `image3` widać wagi = 5, ustawione zapewne właśnie z „Gry admina”).

### Naprawa
Minimalna i bezpieczna: wydzielić `ensureYearMapEntry` do zasięgu globalnego (obok `getDefaultStatsManualFieldValue`, `Main/app.js:167`) i używać jej w obu widokach — albo zduplikować lokalnie w `initStatisticsView`. Wersja globalna jest lepsza, bo obie kopie logiki i tak są identyczne.

### Dodatkowe usterki w tym samym handlerze
- **Kolejność walidacji.** `window.prompt` jest wywoływany **przed** sprawdzeniem `!state.selectedYear` (`Main/app.js:7330-7332`). Jeśli nie wybrano roku, użytkownik i tak zobaczy okno, wpisze wartość, a handler po cichu wyjdzie bez żadnego komunikatu. W wersji z „Gry admina” jest to obsłużone lepiej — `applyBulkWeightValue` ustawia komunikat „Wybierz rok, aby ustawić wagę.” (`Main/app.js:7742-7745`).
- **Brak komunikatu potwierdzającego.** „Gry admina” po udanej operacji pokazuje `Zaktualizowano WEIGHT1 dla N graczy.` (`Main/app.js:7765`). „Statystyki” nie pokazują nic — nawet gdyby zapis się udał, użytkownik nie ma potwierdzenia.

---

## 7. Zgłoszenie 4 — gracz „anonim” zostaje w podsumowaniu gry i w statystykach po zmianie nazwy na „Adolf” — **POTWIERDZONY BŁĄD**

### Mechanizm
Aplikacja przechowuje nazwę gracza w **dwóch niezależnych miejscach**:

1. **Źródło prawdy** — lista graczy w `PLAYER_ACCESS_COLLECTION` (zakładka „Gracze”). To ją edytuje użytkownik.
2. **Kopia (denormalizacja)** — pole `playerName` zapisane w każdym wierszu gry (`Tables/{gra}/rows/{wiersz}` i `UserGames/{gra}/rows/{wiersz}`).

Kopia jest zapisywana **tylko raz**, w momencie wyboru gracza z listy w modalu „Szczegóły” (`Main/app.js:8310-8316`):
```js
playerSelect.addEventListener("change", () => {
  const selectedPlayer = options.find((option) => option.id === playerSelect.value) ?? null;
  void db.collection(gamesCollectionName).doc(gameId).collection(gameDetailsCollectionName).doc(row.id).update({
    playerId: selectedPlayer?.id ?? "",
    playerName: selectedPlayer?.name ?? ""
  });
});
```

Zmiana nazwy w zakładce „Gracze” (`updatePlayerField`, `Main/app.js:6286-6312`) zapisuje **wyłącznie** dokument listy graczy:
```js
player[field] = value;
scheduleDebouncedUpdate(`players:${playerId}:${field}`, () => { void savePlayers(); });
```
Nie ma żadnej propagacji do `rows` w grach ani do `admin_games_stats`.

### Dlaczego w „Szczegółach” nazwa się zmienia, a w podsumowaniu nie

| Widok | Skąd bierze nazwę | Efekt po zmianie nazwy |
|---|---|---|
| Modal „Szczegóły” gry | `<option>` budowane z **żywej** listy `state.playerOptions`, wybór po `playerId` (`Main/app.js:8302-8309`) | ✅ pokazuje nową nazwę („Adolf”) |
| Podsumowanie gry — „Gry admina” | `row.playerName` z dokumentu wiersza (`Main/app.js:8110-8112`) | ❌ stara nazwa („anonim”) |
| Podsumowanie gry — „Gry użytkowników” | `row.playerName` z dokumentu wiersza (`Main/app.js:3134-3136`) | ❌ stara nazwa |
| Statystyki graczy — „Gry admina” | `playerName` przypisany przy pierwszym napotkaniu gracza (`Main/app.js:7786-7797`), renderowany w `Main/app.js:8228` | ❌ stara nazwa |
| Statystyki graczy — „Statystyki” | analogicznie `Main/app.js:6966-6979` | ❌ stara nazwa |
| Ranking | budowany z `playerRows`, więc dziedziczy tę samą nazwę | ❌ stara nazwa |
| Modal „Status potwierdzeń” | `getUniquePlayersFromRows(rows)` → `row.playerName` (`Main/app.js:1725-1739`) | ❌ stara nazwa |
| „Gry do potwierdzenia” (admin) | `normalizePlayerName(rowData.playerName)` (`Main/app.js:3806`) | ❌ stara nazwa |

To potwierdza obserwację użytkownika w 100%: „w szczegółach gry nazwa się zmienia. Jest ok. Ale w podsumowaniu gry jak i w statystykach (Adolfa) już nie ma.”

### Ważna informacja pozytywna
**Dane liczbowe nie są uszkodzone.** Agregacja statystyk odbywa się po kluczu `statsKey`, który dla gracza z ID ma postać `id:<playerId>` (`getStatsKey`, `Main/app.js:6865-6872` i `Main/app.js:7472`+). Zmiana nazwy nie rozbija historii gracza na dwa wiersze — psuje się tylko **wyświetlana etykieta**. To dobra wiadomość, bo naprawa polega wyłącznie na zamianie źródła etykiety, bez migracji liczb.

### Możliwe podejścia do naprawy

- **Podejście „na żywo” (rekomendowane).** W miejscach wyświetlania rozwiązywać nazwę przez `playerId` z aktualnej listy graczy, z zapisanym `playerName` jako fallbackiem dla starych rekordów bez `playerId`. Zaleta: działa natychmiast dla wszystkich historycznych gier, bez ruszania bazy. Wada: wymaga, żeby `initStatisticsView` też subskrybowało listę graczy — obecnie robi to `initAdminGames` (`Main/app.js:8461-8474`) i `initUserGamesManager` (`Main/app.js:3504`), ale **nie** `initStatisticsView`.
- **Podejście „przepisz przy zmianie nazwy”.** Przy edycji nazwy w „Gracze” przejść po wszystkich `rows` we wszystkich grach i podmienić `playerName`. Wady: dużo operacji zapisu (przy 10 grach × 8 graczy to setki dokumentów przy każdym wpisanym znaku, bo pole zapisuje się z debounce po każdej literze), ryzyko rozjazdu przy błędzie w połowie operacji, brak działania wstecz dla rekordów już rozjechanych.
- **Podejście hybrydowe.** Wyświetlanie „na żywo” + jednorazowy skrypt naprawczy dla spójności danych.

### Wariant, którego trzeba się wystrzegać
Gracze **bez** `playerId` (stare rekordy) mają `statsKey = name:<nazwa>`. Dla nich zmiana nazwy w „Gracze” **rozbiłaby** historię na dwóch różnych graczy. Trzeba to obsłużyć osobno — najlepiej najpierw jednorazowo uzupełnić `playerId` w starych wierszach.

---

## 8. Zgłoszenie 5 — eksport XLSX ze „Statystyki”: szersze kolumny, wyśrodkowanie, separator tysięcy

### Stan faktyczny
Cały eksport to 8 linii (`Main/app.js:7367-7393`):
```js
const worksheet = window.XLSX.utils.aoa_to_sheet([headers, ...dataRows]);
const workbook = window.XLSX.utils.book_new();
window.XLSX.utils.book_append_sheet(workbook, worksheet, "Statystyki");
...
window.XLSX.writeFile(workbook, fileName);
```
Brak `!cols` (szerokości), brak stylów komórek, brak formatów liczbowych. Nazwa pliku: `GG-MM-SS_RRRR-MM-DD_Statystyki_ROK.xlsx`.

### Ocena poszczególnych żądań

| Żądanie | Wykonalność | Uwagi |
|---|---|---|
| **Szersze kolumny** | ✅ łatwe | `worksheet["!cols"] = [{ wch: 22 }, ...]` — obsługiwane przez SheetJS Community Edition. Szerokość można policzyć z długości nagłówka i najdłuższej wartości. |
| **Separator tysięcy** | ⚠️ wymaga dodatkowej pracy | Format liczbowy `z: "# ##0"` jest wspierany w CE, **ale tylko dla komórek typu liczbowego**. Obecnie sporo wartości wychodzi jako **tekst** (patrz niżej), więc sam format nic nie da. |
| **Wyśrodkowanie nagłówków i wartości** | ❌ **niewykonalne w obecnej bibliotece** | `Main/index.html:989` ładuje `https://cdn.sheetjs.com/xlsx-0.20.2/package/dist/xlsx.full.min.js` — to **SheetJS Community Edition**, która przy zapisie **ignoruje style komórek** (`s`). Wyrównanie, pogrubienie i wypełnienie to funkcje wersji Pro. |

### Problem z typami danych (blokuje separator tysięcy)
`aoa_to_sheet` ustala typ komórki po typie JS. Sprawdzenie `STATS_COLUMN_CONFIG` (`Main/app.js:132-162`):

- **liczby** (będą `t:'n'`): `championshipCount`, `meetingsCount`, `points` → `row.pointsSum`, `plusMinusSum`, `payoutSum`, `depositsSum`, `playedGamesPoolSum`, `result`;
- **tekst** (będą `t:'s'`, separator ich nie dotknie): wszystkie **wagi** — bo `getDefaultManualFieldValue` zwraca `String` (`Main/app.js:167-173`); oraz **procenty** — `` `${row.participationPercent}%` ``, `` `${row.percentAllGames}%` ``, `` `${row.percentPlayedGames}%` ``.

Czyli żeby separator zadziałał, trzeba dodatkowo:
- wagi konwertować na liczby przy eksporcie,
- procenty albo eksportować jako liczbę z formatem `0"%"`, albo jako ułamek z formatem `0%` (uwaga: to zmieni wartość z `80` na `0,8`), albo zostawić jako tekst i pogodzić się z brakiem separatora w tych kolumnach.

### Opcje realizacji wyśrodkowania
1. **`xlsx-js-style`** — społecznościowy fork SheetJS wspierający style przy zapisie, w praktyce drop-in (to samo API `XLSX.*`). Najmniejsza zmiana w kodzie.
2. **ExcelJS** — pełne wsparcie stylów, ale inne API — trzeba przepisać całą funkcję eksportu; biblioteka jest wyraźnie cięższa.
3. **SheetJS Pro** — licencja płatna.
4. **Rezygnacja z wyśrodkowania** — same szersze kolumny + separator już bardzo poprawiają czytelność.

### Uwaga o zakresie
Eksport jest wspólny dla admina i gracza (`initStatisticsView` wywoływane dwa razy — `Main/app.js:7404-7424`), więc jedna zmiana obsłuży oba profile, zgodnie z nagłówkiem „Profil admina oraz użytkownika”. Różnica: admin eksportuje **wszystkie** kolumny, gracz tylko widoczne dla danego roku (`Main/app.js:7375`).

---

## 9. Zgłoszenie 6 — numeracja kolejności potwierdzeń

### Stan faktyczny
Modal „Status potwierdzeń” (`Main/app.js:1769-1872`) ma dwie kolumny: `Gracz` i `Status`. Lista budowana jest przez `getConfirmationStatusesForRows` (`Main/app.js:1761-1767`):
```js
return getUniquePlayersFromRows(rows).map((player) => ({
  playerName: player.playerName,
  confirmed: confirmedPlayers.has(player.identifier)
}));
```
Kolejność wierszy = kolejność **zapisania graczy do gry** (`rows` sortowane po `createdAt`), a nie kolejność potwierdzeń. Czas potwierdzenia jest całkowicie odrzucany — `getConfirmedPlayerIdentifiersFromConfirmations` (`Main/app.js:1741-1753`) zwraca sam `Set` identyfikatorów.

### Dobra wiadomość: dane czasowe już istnieją
Każde potwierdzenie zapisuje `updatedAt` jako `serverTimestamp()`:
- gracz potwierdza — `Main/app.js:2454-2460`,
- gracz anuluje — `Main/app.js:2469-2476`,
- admin potwierdza — `Main/app.js:3859-3866`,
- admin anuluje — `Main/app.js:3879-3886`.

### Zła wiadomość: `updatedAt` to nie jest czas pierwszego potwierdzenia
`updatedAt` jest nadpisywany przy **każdej** zmianie, w tym przy **anulowaniu**. Scenariusz: Goździk potwierdza jako pierwszy, potem anuluje i potwierdza ponownie → traci pierwsze miejsce w kolejce. Dla reguły „kto pierwszy ten lepszy” przy 12 chętnych na 8 miejsc to jest istotne — a jednocześnie może być pożądane (kto zrezygnował, ten wypada na koniec). To decyzja biznesowa, nie techniczna.

### Rekomendowany model danych
Dodać osobne pole `confirmedAt`, ustawiane **tylko** przy przejściu „niepotwierdzony → potwierdzony” i czyszczone przy anulowaniu. Wtedy:
- numer = pozycja w liście potwierdzonych posortowanej rosnąco po `confirmedAt`,
- `updatedAt` pozostaje jako czas ostatniej zmiany (przydatny do audytu),
- dla rekordów historycznych (bez `confirmedAt`) fallback na `updatedAt`, a gdy i tego brak — na koniec listy.

### Pułapki techniczne
1. **`serverTimestamp()` jest chwilowo `null`.** Firestore zwraca lokalnie `null` do czasu potwierdzenia zapisu przez serwer. Gracz, który właśnie kliknął „Potwierdź”, przez ułamek sekundy zobaczy siebie bez numeru albo na końcu. Trzeba obsłużyć `null` (np. przez `snapshot.metadata.hasPendingWrites`) lub zaakceptować mignięcie.
2. **Modal jest współdzielony.** `getConfirmationsStatusModalController()` (`Main/app.js:1770-1772`) to singleton używany przez trzy miejsca: „Gry admina” (`Main/app.js:7519`), „Gry użytkowników” admina i „Gry Użytkowników” gracza (`Main/app.js:2779`). Dodanie kolumny „Lp.” **automatycznie pojawi się we wszystkich trzech** — to raczej zaleta, ale trzeba mieć świadomość.
3. **Trzeba przekazać całe obiekty potwierdzeń, nie sam `Set`.** Obecny `getConfirmationStatusesForRows` traci informację o czasie — konieczna zmiana sygnatury lub nowa funkcja.
4. **„Gry do potwierdzenia” (admin) to inna tabela.** `initAdminConfirmations` (`Main/app.js:3782-3789`) buduje własną tabelę `Gracz | Status | Akcje` — dodanie numeracji wymaga osobnej zmiany w tym miejscu.
5. **Potwierdzenia admina.** Admin potwierdzający za gracza (`updatedBy: "admin"`) też dostanie numer — trzeba zdecydować, czy to ma być liczone tak samo.

### Ograniczenie zakresu (ważne)
Użytkownik prosi o numerację również dla gier w zakładce **„Gry admina”**. Ale — jak opisano w pkt 5 — potwierdzenia obecności obsługiwane są **wyłącznie dla kolekcji `UserGames`**. Kolumna `IlośćPotwierdzonych` i przycisk `Statusy` w „Gry admina” istnieją, lecz w obecnej wersji aplikacji **nie ma żadnego ekranu, na którym ktokolwiek mógłby potwierdzić grę admina** (patrz pkt 10.1). Numeracja tam będzie więc pusta, dopóki ten temat nie zostanie rozstrzygnięty.

---

## 10. Dodatkowe błędy i problemy znalezione przy przeglądzie (poza zgłoszeniami użytkownika)

### 10.1. Potwierdzenia dla gier admina są martwe, ale UI je pokazuje
Zakładka „Gry admina” pokazuje kolumnę `IlośćPotwierdzonych` z licznikiem `potwierdzeni/zapisani` i przycisk `Statusy` (`Main/app.js:7975-7988`), a kod subskrybuje podkolekcję `confirmations` gier z `Tables` (`Main/app.js:8516`). Tymczasem **żadne miejsce w aplikacji nie zapisuje potwierdzeń do `Tables`**:
- widok gracza „Gry do Potwierdzenia” → tylko `UserGames` (`Main/app.js:2381`),
- widok admina „Gry do potwierdzenia” → tylko `UserGames` (`Main/app.js:3771`),
- „Najbliższa gra” → `nextGamesState.adminGames` na stałe `[]` (`Main/app.js:1908`, `Main/app.js:2020-2021`).

**Skutek:** licznik dla gier admina zawsze pokaże `0/N`, a modal `Statusy` — wszystkich jako „Niepotwierdzony”. Na `image1` gra „GRUBY” pokazuje `4/8`, co sugeruje dane historyczne sprzed rozdzielenia obu obiegów (opisanego w `Main/docs/Documentation.md`, sekcja „Aktualizacja techniczna: separacja Gry admina od potwierdzeń i Najbliższej gry`).

**Rekomendacja:** albo ukryć te elementy dla gier admina, albo świadomie przywrócić obieg potwierdzeń dla `Tables`. Obecny stan wprowadza użytkownika w błąd.

### 10.2. Utrata fokusu przy edycji wag w zakładce „Statystyki”
To dokładnie ten sam typ błędu, który opisuje `Analizy/Wazne_Fokus.md`.

W `initStatisticsView.renderStats` pole edycyjne wagi po każdym wciśniętym znaku wywołuje **pełne przerysowanie tabeli** (`Main/app.js:7169-7186`):
```js
input.addEventListener("input", () => {
  ...
  scheduleDebouncedUpdate(...);
  renderStats();   // <- czyści playersStatsBody.innerHTML i buduje wszystko od nowa
});
```
Dodatkowo pola te **nie mają metadanych fokusu** (`data-focus-target`, `data-section`, `data-table-id`, `data-row-id`, `data-column-key`), a `renderStats` **nie używa** `getFocusedAdminInputState` / `restoreFocusedAdminInputState`.

Dla porównania — bliźniacza tabela w „Gry admina” jest zrobiona poprawnie: pola mają komplet `data-*` (`Main/app.js:8184-8188`), a zamiast przerysowania wołane jest punktowe `updateResultsAndRanking` (`Main/app.js:8168-8177`).

**Skutek:** ręczna edycja wag w zakładce „Statystyki” gubi kursor po każdej wpisanej cyfrze. Wpisanie „25” wymaga ponownego kliknięcia w pole po pierwszej cyfrze.

### 10.3. Wstawianie nazw graczy przez `innerHTML` bez escapowania
Podsumowania gier budują wiersze przez interpolację do `innerHTML`:
- `Main/app.js:8110-8118` („Gry admina”),
- `Main/app.js:3134-3142` („Gry użytkowników”).

```js
tr.innerHTML = `
  <td>${row.playerName || ""}</td>
  ...
`;
```
Nazwa gracza pochodzi z pola tekstowego w zakładce „Gracze”, bez sanityzacji. Nazwa zawierająca `<`, `>` lub `&` (np. `Jan & Ola`, `<b>Żbik`) rozjedzie układ tabeli, a w skrajnym przypadku pozwoli na wstrzyknięcie HTML. Ryzyko bezpieczeństwa jest ograniczone (nazwy ustawia administrator), ale to realny defekt — wszystkie pozostałe komórki w aplikacji są budowane przez `textContent`, który jest bezpieczny.

### 10.4. Nazwa gracza jako identyfikator dokumentu Firestore
W `initAdminConfirmations` (`Main/app.js:3838`):
```js
const confirmationDocId = player.playerId || confirmation?.id || player.playerName;
```
Dla starych wierszy bez `playerId` identyfikatorem dokumentu staje się **nazwa gracza**. Firestore nie dopuszcza w ID znaku `/` ani wartości `.` i `..`, a ID jest wrażliwe na wielkość liter i spacje. Nazwa typu `Jan/Ola` spowoduje błąd zapisu, a zmiana nazwy stworzy „osierocony” dokument potwierdzenia.

### 10.5. Duplikacja logiki statystyk (~450 linii)
`initStatisticsView` (`Main/app.js:6819-7396`) i `initAdminGames` (`Main/app.js:7427-8649`) mają **dwie niezależne kopie** tej samej logiki: `getStatsKey`, `getDetailRows`, `hasIncludedSummaryOrStatsData`, `getPlayersStatistics`, `getDefaultManualFieldValue`, `getComputedResultValue`, serializacja do `admin_games_stats`.

Kopie są dziś **funkcjonalnie równoważne** (sprawdzone linia po linii), ale to właśnie ta duplikacja doprowadziła do błędu z pkt 6 — ktoś przeniósł wywołanie bez przeniesienia definicji. Każda przyszła zmiana reguł liczenia musi być robiona **dwa razy**, inaczej zakładki „Gry admina” i „Statystyki” pokażą różne liczby.

### 10.6. Konkurencyjny zapis do `admin_games_stats`
Obie zakładki zapisują **całą tablicę `rows`** do tego samego dokumentu `admin_games_stats/{rok}`:
- `initStatisticsView.persistYearConfig` (`Main/app.js:6906-6909`) — zapisuje `rows` **i** `visibleColumns`,
- `initAdminGames.persistManualStats` (`Main/app.js:7772`) — zapisuje same `rows`.

Oba widoki są aktywne równocześnie na tej samej stronie. Przy równoległej edycji (albo przy opóźnieniu snapshotu) nowszy zapis może cofnąć zmianę z drugiej zakładki. W praktyce oba stany zbiegają się dzięki wspólnemu `onSnapshot`, ale okno na rozjazd istnieje.

Dodatkowo `initStatisticsView` jest inicjalizowane **dwa razy** (widok admina i gracza — `Main/app.js:7404-7424`), więc dopisanie brakującej konfiguracji `visibleColumns` (`Main/app.js:7245-7252`) wykonuje się podwójnie. Zapis jest idempotentny, więc nie powstaje pętla, ale to zbędny ruch do bazy.

### 10.7. Wydajność: przerysowanie statystyk po każdym snapshotcie każdej gry
`initStatisticsView` zakłada osobny listener na `rows` **każdej gry** i w każdym z nich woła pełne `renderStats()` (`Main/app.js:7274-7279`). Przy 9 grach pierwsze wczytanie strony wywołuje przeliczenie i przerysowanie całej tabeli statystyk co najmniej 9 razy. Analogicznie w `initAdminGames`. Przy kilkudziesięciu grach w roku będzie to zauważalne.

### 10.8. Martwy kod
- `getTablesCollectionName` (`Main/app.js:1300-1306`) jest zdefiniowana, ale **nigdy nie wywoływana** — konfiguracja `tablesCollection` z `config/firebase-config.js` jest ignorowana.
- `Promise.all([])` dla gier admina w `synchronizeNextGamesConfirmations` (`Main/app.js:1907-1910`) — pozostałość po rozdzieleniu obiegów.

### 10.9. Błędy w dokumentacji (naprawione w ramach tego zadania — patrz pkt 11)
- `Main/docs/README.md` §14 opisuje w zakładce admina „Gry do potwierdzenia” akcje **Szczegóły** i **Notatki do gry**, których tam nie ma — `initAdminConfirmations` renderuje wyłącznie `Potwierdź` i `Anuluj` (`Main/app.js:3850-3891`). Te akcje istnieją w **widoku gracza** (`Main/app.js:2485-2508`), skąd najwyraźniej zostały omyłkowo przepisane.
- `Main/docs/README.md` §10.3 pomija kolumnę `IlośćPotwierdzonych` i przycisk `Statusy` w zakładce „Gry admina”.
- `Main/docs/README.md` §13 nie wspomina, że „Najbliższa gra” pokazuje wyłącznie gry użytkowników, ani nie dokumentuje przycisku **Usuń Całkowicie** (`Main/app.js:1970-1980`) — a jest to akcja nieodwracalna.
- `Main/docs/README.md` opis kolumny `CzyZamknięta` nie mówi, na co ta flaga faktycznie wpływa.
- `Main/docs/Documentation.md:131` stwierdza, że „masowe ustawianie wag” odczytuje wpisy po `statsKey` — sugerując, że działa w obu zakładkach. W zakładce „Statystyki” nie działa w ogóle (pkt 6).

---

## 11. Zmiany w dokumentacji wykonane w ramach tego zadania

Kod aplikacji **nie został zmieniony**. Zaktualizowano wyłącznie dokumentację, zgodnie z pkt 9 promptu.

Pełny wykaz zmian z cytowaniem linii przed i po znajduje się w sekcji **11.1**. Moduł `Second` nie wymagał zmian — żadne ze zgłoszeń go nie dotyczy, a jego dokumentacja nie zawiera opisanych wyżej nieścisłości.

### 11.1. Wykaz zmian — `Main/docs/README.md`

**Sekcja „Gry Użytkowników” → Kolumna CzyZamknięta (linia 178)**

Było:
```
- odznacz, aby przywrócić status otwartej.
```
Jest:
```
- odznacz, aby przywrócić status otwartej,
- zaznaczona gra **znika** z zakładki „Gry do potwierdzenia” (u gracza i u administratora) oraz z widoku „Najbliższa gra”,
- flaga **nie wpływa** na podsumowania pod tabelą ani na zakładkę „Statystyki” — do statystyk i rankingu liczone są wszystkie gry z wybranego roku, niezależnie od stanu tego checkboxa.
```
*Powód:* dotychczasowy opis nie mówił, na co ta flaga faktycznie wpływa — to źródło zgłoszenia nr 1.

**Sekcja 9.3 „Edycja istniejącego gracza” (po linii 351) — dodano akapit**

Dodano:
```
> **Gdzie widać zmienioną nazwę gracza.** Nowa nazwa pojawia się od razu na listach wyboru gracza — czyli w modalach **Szczegóły** gry oraz w kalkulatorze. Natomiast **podsumowania pod tabelą gier, tabela statystyk, ranking, okno „Status potwierdzeń” i zakładka „Gry do potwierdzenia” nadal pokazują nazwę zapisaną w momencie dodania gracza do danej gry** — dla już istniejących gier stara nazwa pozostaje. Liczby i historia gracza nie są tym uszkodzone: gracz rozpoznawany jest po swoim ID, a nie po nazwie.
```
*Powód:* udokumentowanie faktycznego zachowania z pkt 7.

**Sekcja 10.3 „Akcje na grze” (linia 379)**

Było:
```
- checkbox **CzyZamknięta** — zamknięcie/otwarcie gry.
```
Jest:
```
- checkbox **CzyZamknięta** — zamknięcie/otwarcie gry; zaznaczona gra znika z „Gry do potwierdzenia” i z „Najbliższa gra”, ale **nie wpływa** na podsumowania, statystyki ani ranking (te liczą wszystkie gry z wybranego roku),
- kolumna **IlośćPotwierdzonych** — licznik `potwierdzeni/zapisani` oraz przycisk **Statusy** otwierający okno tylko do odczytu z listą graczy i ich statusem.

> Uwaga: potwierdzenia obecności zbierane są wyłącznie dla gier z zakładki „Gry użytkowników”. Dla gier z tej zakładki licznik `IlośćPotwierdzonych` i okno **Statusy** pozostają puste (`0/N`, wszyscy „Niepotwierdzony”), o ile w bazie nie ma starszych rekordów potwierdzeń.
```
*Powód:* brakowało opisu kolumny `IlośćPotwierdzonych` i przycisku `Statusy` (pkt 10.1).

**Sekcja 11 „Zakładka admina Statystyki” (linie 416–417)**

Było:
```
4. W tabeli graczy możesz używać przycisków **Waga1–Waga6** do zbiorczego ustawiania wag kolumn (analogicznie jak wyżej); przyciski mają stałą szerokość `8ch`.
5. W panelu **Ranking** kolumny **Miejsce**, **Gracz** i **Wynik** mieszczą się na ekranie bez poziomego przewijania; kolumna **Gracz** jest zwężona, nagłówek **Gracz** jest wyrównany do lewej, a długie nazwy są obcinane wielokropkiem.
```
Jest:
```
4. W tabeli graczy przyciski **Waga1–Waga6** mają stałą szerokość `8ch`. **Uwaga: w tej zakładce zbiorcze ustawianie wag nie działa** — okno z pytaniem o wartość się pojawia, ale wpisana liczba nie jest zapisywana. Aby ustawić wagi zbiorczo, użyj tych samych przycisków w zakładce **„Gry admina”** (pkt 10.6) — wagi są wspólne dla obu zakładek, więc wartość ustawiona tam pojawi się od razu tutaj.
5. Wartości w kolumnach wag możesz też wpisać ręcznie w polach tabeli. W tej zakładce po wpisaniu jednego znaku pole traci kursor — trzeba kliknąć w nie ponownie, aby dopisać kolejną cyfrę (w zakładce „Gry admina” ten problem nie występuje).
6. W panelu **Ranking** kolumny **Miejsce**, **Gracz** i **Wynik** mieszczą się na ekranie bez poziomego przewijania; kolumna **Gracz** jest zwężona, nagłówek **Gracz** jest wyrównany do lewej, a długie nazwy są obcinane wielokropkiem.
```
*Powód:* instrukcja obiecywała działanie, którego nie ma (pkt 6) i nie ostrzegała o utracie fokusu (pkt 10.2). Punkt „Ranking” przenumerowano z 5 na 6.

**Sekcja 13 „Najbliższa gra” (linie 443–449)**

Było:
```
Zakładka automatycznie ukrywa gry starsze niż dzisiejsza data — na liście zobaczysz wyłącznie gry zaplanowane na dziś lub przyszłe terminy.
...
- dane są aktualizowane na podstawie aktywnych gier i potwierdzeń.
```
Jest:
```
Na liście pojawiają się wyłącznie gry z zakładki **„Gry użytkowników”** — gry z zakładki „Gry admina” nie są tu pokazywane.

Zakładka automatycznie ukrywa gry starsze niż dzisiejsza data oraz gry oznaczone jako **CzyZamknięta** — na liście zobaczysz wyłącznie gry otwarte, zaplanowane na dziś lub przyszłe terminy.
...
Kolumny tabeli: **Rodzaj gry**, **Data**, **Nazwa**, **CzyWszyscyPotwierdzili** (`Tak` / `Nie`) oraz **Akcje**.
...
- dane są aktualizowane na podstawie aktywnych gier i potwierdzeń,
- przycisk **Usuń Całkowicie** (kolumna `Akcje`, tylko w widoku administratora) kasuje grę razem ze wszystkimi jej szczegółami i potwierdzeniami — operacja jest natychmiastowa i nieodwracalna, bez dodatkowego potwierdzenia.
```
*Powód:* brakowało informacji o źródle danych, o filtrze `isClosed` i o nieodwracalnym przycisku `Usuń Całkowicie`.

**Sekcja 14 „Gry do potwierdzenia” (linie 457–461) — usunięto błędny opis**

Było:
```
Typowe akcje:
- podgląd, którzy gracze potwierdzili,
- wejście do **Szczegółów** gry,
- otwarcie **Notatek do gry**,
- odświeżanie listy (jeśli widoczne przyciski odświeżenia w sekcji).
```
Jest:
```
Lista obejmuje wyłącznie **otwarte gry z zakładki „Gry użytkowników”** (gry oznaczone jako `CzyZamknięta` są pomijane). Gry z zakładki „Gry admina” nie są tu pokazywane.

Układ sekcji:
- każda gra ma własny nagłówek z nazwą oraz linię `Rodzaj gry | Data`,
- pod nagłówkiem znajduje się tabela z kolumnami **Gracz**, **Status** (`Potwierdzono` / `Niepotwierdzono`) i **Akcje**; wiersze potwierdzone są podświetlone na złoto.

Typowe akcje:
- podgląd, którzy gracze potwierdzili,
- **Potwierdź** — ustawia potwierdzenie obecności w imieniu wybranego gracza,
- **Anuluj** — wycofuje potwierdzenie obecności wybranego gracza,
- odświeżenie listy wykonujesz górnym przyciskiem **Odśwież** w nagłówku panelu administratora (sekcja nie ma własnego przycisku odświeżania).

> W tej zakładce nie ma przycisków **Szczegóły** ani **Notatki do gry** — te akcje dostępne są w sekcji gracza „Gry do Potwierdzenia” (pkt 3.3).
```
*Powód:* opisane akcje **Szczegóły** i **Notatki do gry** w tej zakładce nie istnieją — `initAdminConfirmations` renderuje wyłącznie `Potwierdź` i `Anuluj` (`Main/app.js:3850-3891`).

### 11.2. Wykaz zmian — `Main/docs/Documentation.md`

**Sekcja „Identyfikacja gracza w potwierdzeniach i statystykach” (po linii 131) — dodano dwa punkty**

Dodano:
```
- **Masowe ustawianie wag działa wyłącznie w zakładce „Gry admina”** (`initAdminGames` → `applyBulkWeightValue`, zapis do `admin_games_stats/{rok}`). Handler tych samych przycisków w zakładce „Statystyki” (`initStatisticsView`) wywołuje `ensureYearMapEntry`, która jest zadeklarowana jako `const` lokalnie w `initAdminGames` i nie jest widoczna z tego zasięgu — kliknięcie przerywa się na `ReferenceError` po zamknięciu `window.prompt`, bez zapisu i bez komunikatu w UI. Obie zakładki zapisują do tej samej kolekcji `admin_games_stats`, więc wagi ustawione w „Gry admina” są natychmiast widoczne w „Statystyki”.
- **Nazwa gracza jest zdenormalizowana.** Wiersze gier (`rows`) i rekordy w `admin_games_stats` przechowują kopię `playerName` z chwili zapisu. Aktualizacja nazwy w zakładce „Gracze” (`updatePlayerField` → `savePlayers`) zapisuje wyłącznie dokument `PLAYER_ACCESS_COLLECTION` i nie propaguje się do `rows` ani do statystyk. Listy `<select>` w modalach szczegółów budują etykiety z żywej listy `state.playerOptions` po `playerId`, dlatego pokazują nazwę aktualną; podsumowania (`renderSummaries`), statystyki (`getPlayersStatistics`), ranking, modal „Status potwierdzeń” i `initAdminConfirmations` czytają zapisane `playerName` i pokazują nazwę historyczną. Agregacja odbywa się po `statsKey`, więc rozjazd dotyczy wyłącznie warstwy prezentacji.
```
*Powód:* poprzedni zapis („Ranking statystyk, masowe ustawianie wag i eksport XLSX odczytują wpisy ręczne z mapy rocznej po `statsKey`…”) sugerował, że masowe ustawianie wag działa w obu zakładkach.

**Sekcja „Aktualizacja techniczna: separacja Gry admina od potwierdzeń i Najbliższej gry” (linia 208)**

Było:
```
  - widok `Najbliższa gra` agreguje tylko `UserGames`;
```
Jest:
```
  - widok `Najbliższa gra` agreguje tylko `UserGames` (`nextGamesState.adminGames` pozostaje pustą tablicą);
```

Dodano na końcu sekcji:
```
- Konsekwencja: **żadna ścieżka UI nie zapisuje dokumentów do `Tables/{gameId}/confirmations`**. Zakładka `Gry admina` nadal renderuje kolumnę `IlośćPotwierdzonych` i przycisk `Statusy` oraz subskrybuje tę podkolekcję, więc dla gier admina licznik pokazuje `0/N` (poza rekordami historycznymi sprzed separacji).
```

**Dodano nową sekcję „Zasięg flagi `isClosed`”**

Dodano:
```
## Zasięg flagi `isClosed`
- Pole `isClosed` gry jest odczytywane tylko w dwóch filtrach: `getActiveGamesForConfirmations` (zakładki `Gry do potwierdzenia`) oraz `getCombinedOpenGames` (widok `Najbliższa gra`).
- Selektory `getGamesForSelectedYear` w `initAdminGames`, `initStatisticsView` i `initUserGamesManager` filtrują wyłącznie po roku (oraz po dostępie w widoku gracza), dlatego `renderSummaries`, `getPlayersStatistics` i ranking obejmują **wszystkie** gry wybranego roku niezależnie od `isClosed`.
```

### 11.3. `Second/docs` — bez zmian

Moduł `Second` nie zawiera żadnej z opisanych funkcji (brak `isClosed`, brak wag, brak `admin_games_stats`, brak eksportu XLSX, brak potwierdzeń obecności), a jego dokumentacja nie zawiera nieścisłości powiązanych z tymi zgłoszeniami. Zgodnie z zasadą „nie przechowuj archiwalnych informacji” nie dopisano tam żadnych wzmianek o problemach modułu `Main`.

---

## 12. Podsumowanie techniczne — priorytety

> Kolejność realizacji została zatwierdzona przez użytkownika — patrz **sekcja 14.7**.

| # | Zgłoszenie | Typ | Trudność | Wymaga decyzji użytkownika |
|---|---|---|---|---|
| 3 | Przyciski „Waga” w „Statystyki” | **Błąd** (`ReferenceError`) | Bardzo mała | Nie |
| 4 | Nazwa gracza w podsumowaniu i statystykach | **Błąd** | Średnia | Tak (zakres) |
| 1 | „CzyZamknięta” a statystyki | Brak reguły | Mała | **Tak (kluczowe)** |
| 5 | Eksport XLSX | Ulepszenie | Średnia / duża | Tak (biblioteka) |
| 6 | Numeracja potwierdzeń | Nowa funkcja | Średnia | Tak (reguła) |
| 2 | Przycisk „Importuj” | Nowa funkcja | Duża | **Tak (kluczowe)** |
| 10.2 | Utrata fokusu przy wagach | **Błąd** | Mała | Nie |
| 10.1 | Martwe potwierdzenia gier admina | Niespójność | Mała / duża | Tak |
| 10.3 | `innerHTML` z nazwą gracza | **Błąd** | Bardzo mała | Nie |
| 10.4 | Nazwa gracza jako ID dokumentu | **Błąd** | Mała | Nie |

---

## 13. Wyjaśnienie prostym językiem (dla osoby nietechnicznej)

### 13.1. Czy to da się naprawić?

**Tak, wszystko da się naprawić.** Ale rzeczy różnią się bardzo trudnością — od „poprawka na pięć minut” po „trzeba najpierw ustalić zasady”.

**Rzeczy zepsute — naprawa jest prosta i pewna:**

- **Przyciski „Waga” w zakładce „Statystyki”.** Znalazłem dokładną przyczynę. To literalnie jedna linijka kodu, która próbuje sięgnąć po narzędzie leżące w innej „szufladzie” programu — i go tam nie znajduje. Program się w tym miejscu wywraca po cichu: okienko z pytaniem o wartość się pokazuje, wpisana liczba nigdzie nie trafia, komunikat o błędzie leci tylko do konsoli przeglądarki, której nikt nie ogląda. Naprawa: przenieść to narzędzie do „wspólnej szuflady”. Mała, bezpieczna zmiana.
  *Do czasu naprawy jest obejście:* te same wagi ustawione w zakładce **„Gry admina”** zapisują się w tym samym miejscu i **od razu widać je w „Statystyki”**. Efekt jest identyczny.

- **Znikające „Adolf”, zostające „anonim”.** Aplikacja zapisuje nazwę gracza w dwóch miejscach: na liście graczy (to Pan edytuje) i jako „odbitkę” wklejoną do każdej gry w chwili dopisywania gracza. Modal „Szczegóły” patrzy na listę na żywo — dlatego widać „Adolf”. Podsumowanie i statystyki czytają odbitkę — dlatego zostaje „anonim”. Naprawa: kazać podsumowaniu i statystykom też patrzeć na listę na żywo.
  **Dobra wiadomość:** liczby, punkty i historia gracza **nie są uszkodzone**. Aplikacja rozpoznaje gracza po ukrytym numerze ID, a nie po nazwie. Zła jest tylko wyświetlana etykieta — nic nie trzeba przeliczać od zera.

- **Gubienie kursora przy ręcznym wpisywaniu wag** (znalezione przy okazji, nie było zgłoszone). W zakładce „Statystyki” po wpisaniu jednej cyfry kursor wyskakuje z pola. To ten sam problem, który był już kiedyś naprawiany w innych miejscach aplikacji — tu został pominięty.

**Rzeczy, które nie są zepsute, tylko nigdy nie zostały zbudowane:**

- **Checkbox „CzyZamknięta”.** Sam „ptaszek” działa i zapisuje się poprawnie. Ale w programie **nigdzie nie ma zapisanej reguły**, że ten ptaszek ma cokolwiek zmieniać w statystykach. Dziś robi tylko jedno: gra z ptaszkiem znika z listy „Gry do potwierdzenia” i z „Najbliższej gry”. Do statystyk wchodzą wszystkie gry z danego roku, zawsze. Żeby to zmienić, muszę najpierw wiedzieć **którą** regułę wpisać — bo są dwie przeciwstawne i obie brzmią sensownie (pytanie 1 poniżej).

- **Przycisk „Importuj”.** To nowa funkcja, nie naprawa. Ma sens, bo odkryłem przy okazji rzecz, która może być zaskoczeniem: **gry stworzone przez graczy w ogóle nie wchodzą do statystyk ani do rankingu.** Zakładka „Statystyki” liczy wyłącznie gry z zakładki „Gry admina”. Przycisk „Importuj” byłby jedyną drogą, żeby gra gracza się policzyła.

- **Numeracja kolejności potwierdzeń.** Też nowa funkcja. Dobra wiadomość: aplikacja **już teraz zapisuje godzinę** każdego potwierdzenia — nie trzeba nic dobudowywać w bazie od zera. Zła: zapisuje godzinę *ostatniej zmiany*, a nie *pierwszego potwierdzenia*. Jeśli ktoś potwierdzi, potem anuluje, a potem potwierdzi jeszcze raz — dostanie nowy, gorszy numer.

**Rzecz, która częściowo się nie da — trzeba to powiedzieć wprost:**

- **Wyśrodkowanie w Excelu.** Aplikacja używa darmowej wersji biblioteki, która tworzy pliki Excela. Ta wersja **celowo nie potrafi ustawiać wyglądu komórek** — ani wyśrodkowania, ani pogrubienia, ani kolorów. To nie jest kwestia napisania kodu; ta funkcja po prostu w niej nie istnieje. Żeby wyśrodkować, trzeba **wymienić bibliotekę na inną**. Jest darmowa alternatywa, która to umie (`xlsx-js-style`) i podłącza się prawie bez przeróbek — ale to podmiana ważnego elementu aplikacji, więc trzeba potem sprawdzić, czy eksport nadal działa poprawnie.
  Natomiast **szersze kolumny** i **separator tysięcy** da się zrobić w obecnej bibliotece, bez żadnej wymiany.

### 13.2. Nieoczekiwane konsekwencje — czyli „zmiana X spowoduje, że Y”

**Checkbox „CzyZamknięta”:**

- Jeśli ustalimy, że **do statystyk wchodzą tylko gry zaznaczone** → wszystkie dzisiejsze liczby się zmienią. Na Pana zrzucie ekranu z 9 gier zaznaczonych jest 6, więc „Liczba gier” spadnie z 9 na 6, pula spadnie, a **ranking się przetasuje**. Nikt nic nie skasował — po prostu zaczęły obowiązywać nowe zasady liczenia.
- Jeśli ustalimy odwrotnie, że **gry zaznaczone są pomijane** → w statystykach zostaną 3 gry z 9 i efekt będzie jeszcze bardziej drastyczny.
- Cokolwiek wybierzemy: **odznaczenie lub zaznaczenie jednej gry natychmiast zmieni czyjeś miejsce w rankingu.** Warto o tym uprzedzić graczy przed wdrożeniem.
- Uwaga dodatkowa: dziś zaznaczenie „zamknięta” powoduje, że gra **znika z „Gry do potwierdzenia”**. Jeżeli reguła będzie brzmiała „zamknięta = liczy się do statystyk”, to znaczy, że każda policzona gra jednocześnie przestaje przyjmować potwierdzenia. Zwykle to logiczne (gra rozegrana), ale trzeba to świadomie zaakceptować.

**Poprawka nazw graczy („anonim” → „Adolf”):**

- Po naprawie **stare, zakończone gry też zmienią nazwy w podsumowaniach.** Jeżeli ktoś wydrukował lub zapisał podsumowanie sprzed roku, po naprawie ten sam ekran pokaże inne nazwiska. Nazwa zawsze będzie ta aktualna z listy graczy — historyczna zniknie bezpowrotnie z widoku.
- Jeśli ktoś użyje listy graczy jako „recyklingu” (zmieni nazwę gracza X na zupełnie inną osobę, zamiast dodać nowego gracza), to **wszystkie historyczne gry starego gracza zostaną podpisane nazwiskiem nowego**. Dziś taki zabieg działał — bo stare gry trzymały odbitkę starej nazwy. Po naprawie przestanie.
- **Statystyki nie zmienią się liczbowo** — zmienią się wyłącznie podpisy. To akurat pewne.

**Przycisk „Importuj”:**

- Import to **kopiowanie, nie przenoszenie** (chyba że ustalimy inaczej). Gra będzie istniała w dwóch miejscach naraz. Jeśli gracz później poprawi swoją wersję (np. wpisze poprawną wypłatę), **kopia w statystykach się nie zaktualizuje** — będzie pokazywać starą wersję.
- **Dwukrotne kliknięcie „Importuj” zrobi dwie kopie**, a to podwoi statystyki wszystkich graczy z tej gry. Trzeba wbudować blokadę, żeby to było niemożliwe.
- **Zaimportowana gra wypadnie z obiegu potwierdzeń.** Potwierdzanie obecności działa tylko dla gier użytkowników. Kopia w „Gry admina” zniknie z „Gry do potwierdzenia” i z „Najbliższej gry” — nikt już w niej nie potwierdzi udziału.
- Import **zmieni ranking od razu**, bo dojdą nowi gracze i nowe wyniki. Nowi gracze dostaną domyślną wagę `1`, podczas gdy dotychczasowi mogą mieć np. `5` — więc **trzeba będzie ponownie kliknąć przyciski „Waga”**, żeby wyrównać.
- Jeśli importowana gra ma datę z innego roku niż aktualnie wybrany w panelu „Lata”, **po imporcie zniknie z ekranu** — trafi pod inny rok. To wygląda jak błąd, a nim nie jest.

**Numeracja potwierdzeń:**

- Numery pojawią się **we wszystkich trzech miejscach naraz**, gdzie używane jest okno „Status potwierdzeń” (Gry admina, Gry użytkowników u admina, Gry Użytkowników u gracza) — bo to jedno i to samo okno. To raczej zaleta, ale warto wiedzieć.
- **Dla gier w zakładce „Gry admina” numeracja będzie pusta.** W obecnej wersji aplikacji nie ma żadnego ekranu, na którym gracz mógłby potwierdzić grę admina — kolumna „IlośćPotwierdzonych” i przycisk „Statusy” są tam obecne, ale zawsze pokażą zero. To osobny problem, który znalazłem przy okazji (pkt 10.1).
- **Historyczne potwierdzenia dostaną numery przybliżone.** Aplikacja zapisywała dotąd czas ostatniej zmiany, nie czas pierwszego potwierdzenia. Dla większości przypadków wyjdzie dobrze, ale jeśli ktoś kiedyś anulował i potwierdzał ponownie, jego numer będzie za wysoki. Od momentu wdrożenia poprawki nowe potwierdzenia będą już numerowane dokładnie.
- Jeśli **Pan jako admin** potwierdzi obecność za gracza, ten gracz też dostanie numer w kolejce — z chwili Pana kliknięcia, nie z chwili, gdy się zgłosił telefonicznie.

**Eksport do Excela:**

- Wymiana biblioteki, żeby dało się wyśrodkować, to **podmiana istotnego elementu aplikacji**. Ryzyko jest umiarkowane i odwracalne, ale po zmianie trzeba przetestować eksport na obu profilach (admin i gracz).
- Separator tysięcy zadziała tylko tam, gdzie wartość jest **liczbą**. Dziś część kolumn wychodzi do Excela jako **tekst** — wszystkie kolumny „Waga” oraz wszystkie procenty. Żeby separator objął też je, trzeba dodatkowo zmienić sposób ich zapisu. Skutek uboczny: **wartości procentowe mogą zacząć zachowywać się w Excelu inaczej** (np. `80%` zamiast tekstu `80%` — i wtedy dadzą się sortować i sumować, ale mogą wyświetlić się jako `0,8`, jeśli źle dobrać format).

### 13.3. Pytania doprecyzowujące (prostym językiem)

> **STATUS: wszystkie 13 pytań ma odpowiedź.** Odpowiedzi użytkownika wpłynęły w pliku `Analizy/pop2.docx` (2026-09-07) — komplet wraz z decyzjami wykonawczymi znajduje się w **sekcji 14**. Treść pytań poniżej pozostawiona dla kontekstu.

**Pytanie 1 — najważniejsze. Co ma robić „ptaszek” w kolumnie „CzyZamknięta”?**
Proszę wybrać jedno zdanie, które najlepiej opisuje, o co Panu chodzi:
- **(a)** „Zaznaczam grę, gdy jest rozliczona. **Tylko zaznaczone** gry mają się liczyć do statystyk i rankingu.”
- **(b)** „Zaznaczam grę, gdy chcę ją schować do archiwum. **Zaznaczone** gry mają **przestać** się liczyć do statystyk.”
- **(c)** „Ptaszek ma zostać jak jest (tylko oznaczenie), a do sterowania statystykami zróbmy osobny przycisk/kolumnę.”

**Pytanie 2. Czy przy zmianie reguły z pytania 1 zgadza się Pan, że dzisiejsze statystyki i ranking się zmienią?**
Na Pana zrzucie ekranu z 9 gier zaznaczonych jest 6. Przy wariancie (a) liczba gier spadnie z 9 na 6, przy wariancie (b) — z 9 na 3. Kolejność w rankingu się przetasuje. Czy to jest w porządku, czy wolałby Pan, żebym najpierw pokazał, jak wyjdą nowe liczby, zanim cokolwiek zmienimy?

**Pytanie 3. „Importuj” — kopiuje czy przenosi?**
- **(a)** Gra ma **zostać** u gracza i dodatkowo pojawić się u admina (dwie kopie, każda żyje własnym życiem).
- **(b)** Gra ma **przenieść się** do admina i **zniknąć** z listy gracza (jeden rekord, brak duplikatów).

**Pytanie 4. Czy zaimportowaną grę będzie Pan chciał dalej edytować?**
Jeśli gracz po imporcie poprawi swoją wersję gry, czy kopia u admina ma się sama zaktualizować (trudniejsze, ale możliwe), czy import to ma być „zdjęcie” stanu z danej chwili i dalsze zmiany gracza już go nie dotyczą (prostsze)?

**Pytanie 5. Co po imporcie z potwierdzeniami obecności?**
Po przeniesieniu do „Gry admina” gra znika z ekranu, na którym gracze klikają „Potwierdź”. Czy to Panu odpowiada (bo importuje Pan gry już rozegrane), czy potwierdzenia mają nadal działać?

**Pytanie 6. Numeracja — co się dzieje, gdy ktoś anuluje i potwierdzi ponownie?**
- **(a)** Traci swoje miejsce i idzie na koniec kolejki (uczciwe wobec tych, którzy czekali).
- **(b)** Zachowuje pierwotne miejsce (liczy się, kto pierwszy się zgłosił w ogóle).

**Pytanie 7. Jak mają wyglądać wiersze w oknie „Status potwierdzeń”?**
- **(a)** Kolejność wierszy zostaje jak dziś (kolejność zapisania do gry), a numer to tylko dodatkowa informacja w nowej kolumnie.
- **(b)** Wiersze mają się **przestawić** — potwierdzeni na górze, w kolejności potwierdzania, niepotwierdzeni pod spodem.

**Pytanie 8. Co z niepotwierdzonymi w kolumnie z numerem?**
Puste pole, myślnik `–`, czy coś innego?

**Pytanie 9. Excel — czy wymieniamy bibliotekę dla samego wyśrodkowania?**
- **(a)** Tak, wyśrodkowanie jest ważne — proszę wymienić bibliotekę (trochę większe ryzyko, trzeba przetestować eksport).
- **(b)** Nie — wystarczą szersze kolumny i separator tysięcy, wyśrodkowanie odpuszczamy (zmiana bezpieczna).

**Pytanie 10. Excel — czy procenty mają być prawdziwymi liczbami?**
Dziś kolumny `% udział`, `% Rozegranych gier`, `% Wszystkich gier` trafiają do Excela jako **tekst** — nie da się ich sortować ani sumować. Czy zmienić je na prawdziwe liczby procentowe (wtedy Excel będzie umiał na nich liczyć), czy zostawić jak jest?

**Pytanie 11. Nazwy graczy — czy zgadza się Pan, że zmiana nazwy przepisze też historię?**
Po naprawie zmiana nazwy w zakładce „Gracze” zmieni tę nazwę **we wszystkich grach, także w tych sprzed lat**. Czy to jest to, o co Panu chodzi? Czy może stare, rozliczone gry mają zachować nazwę, która była w nich w chwili gry?

**Pytanie 12. Czy naprawić rzeczy, których Pan nie zgłaszał?**
Znalazłem przy okazji kilka usterek (pkt 10). Czy mam je poprawić przy okazji, czy trzymać się ściśle listy z dokumentu? Chodzi głównie o:
- gubienie kursora przy ręcznym wpisywaniu wag w „Statystyki”,
- licznik potwierdzeń w „Gry admina”, który zawsze pokaże zero (bo nikt nie może tam potwierdzić),
- nazwy graczy ze znakami specjalnymi (`&`, `<`, `/`), które mogą rozsypać tabelę albo uniemożliwić zapis potwierdzenia.

**Pytanie 13. W jakiej kolejności to robimy?**
Proponuję zacząć od trzech rzeczy tanich i pewnych, które nic nie psują: **przyciski „Waga”**, **nazwy graczy** i **gubienie kursora**. Dopiero potem, po Pana odpowiedziach, wziąć się za „CzyZamknięta”, „Importuj”, numerację i Excel. Czy taka kolejność Panu odpowiada?

---

## 14. Odpowiedzi użytkownika i ustalenia wykonawcze (aktualizacja 2026-09-07)

### 14.1. Prompt użytkownika (zachowany dla kontekstu)

> Wgrałem odpowiedzi użytkownika jako Analizy/pop2.docx (część odpowiedzi to komentarz a część to zaznaczenie kolorem wariantu).
> Zaktualizuj analizę Analizy/Uwagi_2026-09-03.md i sprawdź czy wszystko jest już gotowe do wprowadzenia zmian w kodzie aplikacji.

Uzupełnienie promptu w trakcie realizacji:

> jeżeli są jakieś dodatkowe pytanie to też je zapisz w zaktualizowanej analizie (też prostym językiem).

Źródło odpowiedzi: `Analizy/pop2.docx` (dokument bez obrazów; wybrane warianty oznaczone **podświetleniem zielonym**, pozostałe odpowiedzi wpisane jako komentarz tekstowy).

### 14.2. Komplet odpowiedzi

| # | Pytanie | Odpowiedź | Forma |
|---|---|---|---|
| 1 | Co ma robić „CzyZamknięta”? | **(a)** „Zaznaczam grę, gdy jest rozliczona. **Tylko zaznaczone** gry mają się liczyć do statystyk i rankingu.” | podświetlenie |
| 2 | Zgoda na zmianę dzisiejszych liczb? | „Wariant a” | komentarz |
| 3 | „Importuj” kopiuje czy przenosi? | **(a)** kopiuje — gra zostaje u gracza i dodatkowo pojawia się u admina | podświetlenie |
| 4 | Czy kopia ma się sama aktualizować? | „**1 opcja jeśli jest możliwa**” → kopia ma się sama aktualizować, **o ile to wykonalne** | komentarz |
| 5 | Co po imporcie z potwierdzeniami? | „1 opcja” → odpowiada mu, że gra wypada z ekranu potwierdzeń | komentarz |
| 6 | Anuluj + potwierdź ponownie? | **(a)** traci miejsce, idzie na koniec kolejki | podświetlenie |
| 7 | Układ wierszy w „Status potwierdzeń”? | **(b)** wiersze się przestawiają — potwierdzeni na górze w kolejności potwierdzania, niepotwierdzeni pod spodem | podświetlenie |
| 8 | Niepotwierdzeni w kolumnie numeru? | „Puste pole” | komentarz |
| 9 | Excel — wymieniamy bibliotekę? | **(a)** tak, wyśrodkowanie jest ważne | podświetlenie |
| 10 | Procenty jako prawdziwe liczby? | „Zostawić jak jest” → procenty pozostają tekstem | komentarz |
| 11 | Zmiana nazwy przepisze historię? | „zmienić nazwę we wszystkich grach” → tak, nazwa aktualna wszędzie | podświetlenie |
| 12 | Naprawić usterki spoza zgłoszeń? | „napraw” | komentarz |
| 13 | Kolejność prac? | „tak” → zgoda na proponowaną kolejność | komentarz |

### 14.3. Decyzje wykonawcze wynikające z odpowiedzi

#### Zgłoszenie 1 — „CzyZamknięta” (Q1 = a, Q2 = potwierdzone)

Reguła do wdrożenia: **do statystyk, rankingu, „Liczby gier” i „Łącznej puli” wchodzą wyłącznie gry z `isClosed === true`.**

Zakres zmiany — cztery miejsca, które muszą zostać zmienione **spójnie**, bo logika jest zduplikowana (pkt 10.5):
- `initAdminGames.getPlayersStatistics` (`Main/app.js:7774`) — sekcja statystyk i ranking w „Gry admina”,
- `initStatisticsView.getPlayersStatistics` (`Main/app.js:6949`) — zakładka „Statystyki” (admin **i** gracz),
- agregaty `Liczba gier` / `Łączna pula` w obu widokach (`Main/app.js:7117` oraz `Main/app.js:8146-8147`),
- `buildRankingRowsFromStatistics` korzysta z `playerRows`, więc dziedziczy filtr automatycznie.

**Czego filtr NIE obejmuje** (decyzja projektowa, wynika wprost z logiki obsługi):
- **tabela gier** w „Gry admina” — musi nadal pokazywać wszystkie gry, otwarte i zamknięte; inaczej gry nie dałoby się zamknąć, bo zniknęłaby z listy zanim admin zdążyłby kliknąć checkbox,
- **panel „Lata”** — rok musi być widoczny nawet gdy żadna gra w nim nie jest jeszcze zamknięta,
- **podsumowania pod tabelą** („Podsumowanie gry X”) — to widok pojedynczej gry, potrzebny właśnie do weryfikacji **przed** zamknięciem.

Innymi słowy: filtr działa wyłącznie na **agregatach**, nie na listach.

> **Uwaga do Q2 — czego nie mogę zrobić.** Nie jestem w stanie pokazać z góry, jak wyjdą nowe liczby, bo dane leżą w Firestore (projekt `karty-turniej`), do którego nie mam dostępu z tego środowiska. Mogę natomiast zapewnić, że **zmiana niczego nie kasuje** — modyfikuje wyłącznie filtr agregacji po stronie przeglądarki. Odznaczenie checkboxa natychmiast przywraca poprzedni stan liczb, a cofnięcie całej zmiany to jeden commit.

#### Zgłoszenie 3 — przyciski „Waga” (bez pytań, gotowe)
`ensureYearMapEntry` (`Main/app.js:7730`) do zasięgu globalnego, obok `getDefaultStatsManualFieldValue` (`Main/app.js:167`); oba widoki korzystają z jednej implementacji. Dodatkowo: walidacja `state.selectedYear` **przed** `window.prompt` i komunikat potwierdzający po udanym zapisie (jak w „Gry admina”, `Main/app.js:7765`).

#### Zgłoszenie 4 — nazwy graczy (Q11 = nazwa aktualna wszędzie)
Wdrożenie „na żywo”: etykieta gracza rozwiązywana po `playerId` z bieżącej listy graczy, z zapisanym `playerName` jako fallbackiem.

Miejsca do zmiany: `renderSummaries` ×2 (`Main/app.js:8110`, `Main/app.js:3134`), `getPlayersStatistics` ×2, ranking, `getUniquePlayersFromRows` (`Main/app.js:1725`), `initAdminConfirmations` (`Main/app.js:3806`) oraz eksport XLSX.

Wymóg techniczny: `initStatisticsView` **nie subskrybuje dziś listy graczy** — trzeba dodać nasłuch `PLAYER_ACCESS_COLLECTION` (wzorzec: `Main/app.js:8461-8474`).

Ograniczenie do odnotowania: wiersze **bez** `playerId` (rekordy sprzed wprowadzenia ID) nie dadzą się rozwiązać na żywo i zachowają nazwę historyczną. Opcjonalne jednorazowe uzupełnienie `playerId` w starych wierszach — poza zakresem tej zmiany, do decyzji później.

#### Zgłoszenie 5 — eksport XLSX (Q9 = a, Q10 = zostawić)
- Podmiana biblioteki na `xlsx-js-style` (szczegóły i dowód działania w pkt 14.5),
- wyśrodkowanie nagłówków i wartości (poziomo i pionowo),
- szerokości kolumn liczone z długości nagłówka i najdłuższej wartości,
- separator tysięcy `# ##0` na komórkach liczbowych,
- **procenty zostają tekstem** zgodnie z Q10 — separator ich nie dotyczy (i tak są w zakresie 0–100),
- **wagi również zostają tekstem** — to konsekwencja tej samej decyzji; są to małe liczby całkowite, więc separator jest bezprzedmiotowy, a wyśrodkowanie działa na komórkach tekstowych tak samo.

#### Zgłoszenie 6 — numeracja potwierdzeń (Q6 = a, Q7 = b, Q8 = puste pole)
Model danych: nowe pole `confirmedAt` w dokumencie potwierdzenia:
- ustawiane `serverTimestamp()` **tylko** przy przejściu „niepotwierdzony → potwierdzony”,
- **kasowane** (`FieldValue.delete()`) przy anulowaniu — to realizuje Q6(a): ponowne potwierdzenie nadaje nowy znacznik i gracz ląduje na końcu kolejki,
- `updatedAt` zostaje bez zmian jako czas ostatniej modyfikacji.

Wzorzec `FieldValue.delete()` jest już używany w kodzie (`Main/app.js:58`, `3071`, `3276`, `8047`), więc nie wprowadza nowej techniki.

Cztery miejsca zapisu do objęcia zmianą: `Main/app.js:2454` i `2469` (gracz potwierdza / anuluje) oraz `Main/app.js:3859` i `3879` (admin potwierdza / anuluje).

Prezentacja (Q7 = b): potwierdzeni na górze, posortowani rosnąco po `confirmedAt`, z numerem `1, 2, 3…` w nowej kolumnie **przed** kolumną `Gracz`; niepotwierdzeni pod spodem z **pustą** komórką numeru (Q8).

Dwa miejsca w UI: współdzielony modal „Status potwierdzeń” (`Main/app.js:1769`, obsługuje od razu wszystkie trzy zakładki, które go używają) oraz tabela w adminowej „Gry do potwierdzenia” (`Main/app.js:3782`).

Rekordy historyczne: potwierdzenia bez `confirmedAt` dostaną numer na podstawie `updatedAt`; przy braku obu — trafiają na koniec grupy potwierdzonych.

#### Zgłoszenie 2 — „Importuj” (Q3 = a, Q5 = 1) — **patrz pkt 14.4, tu jest blokada**

Co jest już rozstrzygnięte:
- import **kopiuje** (Q3a) — gra zostaje w „Gry użytkowników”,
- konsekwencja pozytywna: skoro gra **zostaje** u gracza, to **nie wypada z obiegu potwierdzeń** — pierwotna obawa z pkt 5 (ryzyko 3) znika sama. Odpowiedź na Q5 („odpowiada mi”) jest tym bardziej spełniona: gracze dalej potwierdzają udział w swojej wersji gry, a kopia u admina po prostu w tym obiegu nie uczestniczy.

#### Punkt 10 — usterki spoza zgłoszeń (Q12 = „napraw”)

Do naprawy, zgodnie z listą przytoczoną w pytaniu:
- **10.2** utrata fokusu przy ręcznej edycji wag w „Statystyki” — dodanie metadanych `data-*` i zastąpienie pełnego `renderStats()` punktową aktualizacją (wzorzec: `updateResultsAndRanking`, `Main/app.js:8168`),
- **10.3** `innerHTML` z nieescapowaną nazwą gracza w obu podsumowaniach — przejście na `textContent`,
- **10.4** nazwa gracza jako identyfikator dokumentu Firestore (`Main/app.js:3838`) — sanityzacja / wymuszenie `playerId`,
- **10.1** martwy licznik potwierdzeń w „Gry admina” — **wymaga jednej decyzji, patrz pkt 14.8, pytanie B**.

Poza listą z pytania (moja rekomendacja, nie blokują niczego):
- **10.8** martwy kod (`getTablesCollectionName`, `Promise.all([])`) — usunąć przy okazji, zmiana zerowego ryzyka,
- **10.5** duplikacja ~450 linii logiki statystyk — **odradzam** scalanie w tej samej turze co zmiany zachowania; najpierw wdrożyć i sprawdzić zmiany funkcjonalne, refaktor osobno,
- **10.6** konkurencyjny zapis do `admin_games_stats` i **10.7** wydajność renderu — zostawić na później, nie przeszkadzają w bieżących pracach.

### 14.4. Blokada: odpowiedzi na Q3 i Q4 są ze sobą sprzeczne

> **STATUS: ROZWIĄZANE.** Użytkownik wybrał wariant **(b)** i zaproponował trzecie podejście, które usuwa sprzeczność u źródła — import następuje raz, w chwili zamknięcia gry przez gracza. Pełna analiza w **pkt 15.3**. Poniższy opis pozostawiony jako zapis toku rozumowania.

**Q3 = (a)**: „dwie kopie, **każda żyje własnym życiem**”.
**Q4 = opcja 1**: „kopia u admina ma się **sama zaktualizować**”.

To są wzajemnie wykluczające się wymagania — kopia nie może jednocześnie żyć własnym życiem i być lustrem oryginału. Użytkownik dopisał „**jeśli jest możliwa**”, więc poniżej odpowiedź, co jest technicznie wykonalne, a co się wyklucza.

**Co jest wykonalne.** Sama automatyczna synchronizacja — tak. Aplikacja nie ma backendu (żadnych Cloud Functions; całość to JavaScript w przeglądarce + Firestore), więc kopiowanie zmian może się wykonać tylko wtedy, gdy ktoś ma otwarty panel administratora. W praktyce oznacza to: zmiany gracza dogonią kopię przy najbliższym otwarciu panelu i dalej na żywo, dopóki panel jest otwarty. Dla tego zastosowania to wystarcza.

**Gdzie jest realny konflikt.** Dwa miejsca, w których automatyczna synchronizacja **zniszczyłaby** ustalenia z innych odpowiedzi:

1. **Kolizja z Q1(a).** Skoro tylko gry zaznaczone jako `CzyZamknięta` liczą się do statystyk, admin musi móc zaznaczyć checkbox na kopii. Jeżeli `isClosed` byłoby synchronizowane ze źródła, każde odświeżenie z gry gracza (u którego gra jest otwarta) **odznaczałoby** checkbox i gra wypadałaby ze statystyk. Wynik: admin nie byłby w stanie utrzymać gry w statystykach.
2. **Kolizja z ręczną edycją.** Dziś admin może edytować zaimportowaną grę w „Gry admina” (skład, wpisowe, wypłaty). Przy pełnej synchronizacji każda taka poprawka zostałaby nadpisana przy następnym odświeżeniu ze źródła.

**Rekomendowane rozwiązanie — synchronizacja selektywna.** Kopia ma dwie kategorie pól:

| Kategoria | Pola | Zachowanie |
|---|---|---|
| **Lustrzane** (synchronizowane ze źródła) | `gameType`, `gameDate`, `name`, cała podkolekcja `rows` (skład, wpisowe, rebuy, wypłata, punkty, mistrzostwo), `confirmations` | aktualizują się automatycznie; **niedostępne do ręcznej edycji w kopii** (pola tylko do odczytu) |
| **Własne kopii** (nigdy nie nadpisywane) | `isClosed`, `postGameNotes`, wagi i wpisy w `admin_games_stats` | ustawia je wyłącznie admin |

Do tego przycisk **„Odłącz”** na kopii: jednorazowo zrywa powiązanie ze źródłem, po czym kopia staje się w pełni edytowalna i niezależna — dokładnie „żyje własnym życiem” w rozumieniu Q3(a). To daje obie rzeczy naraz, tylko rozłożone w czasie: dopóki gra jest podpięta, jest wierna oryginałowi; po odpięciu jest samodzielna.

**Rozwiązanie alternatywne — prostsze i bezpieczniejsze.** Kopia od razu w pełni edytowalna (jak dziś każda gra admina) + przycisk **„Aktualizuj z gry gracza”**, który na żądanie pobiera świeżą wersję. To nie jest „samo się aktualizuje”, ale admin ma pełną kontrolę i nic mu nie nadpisuje pracy. Mniej kodu, brak ryzyka utraty ręcznych poprawek, brak wyścigów przy dwóch otwartych panelach.

Wybór między tymi dwoma wariantami to pytanie **A** w pkt 14.8. **Do czasu odpowiedzi zgłoszenie 2 („Importuj”) nie wchodzi do realizacji** — reszta prac jest od niego niezależna i może ruszyć od razu.

Niezależnie od wyboru, w obu wariantach zostaje wbudowane:
- znacznik `importedFromUserGameId` na kopii + blokada ponownego importu tej samej gry (ochrona przed podwojeniem statystyk),
- komunikat, gdy data importowanej gry należy do innego roku niż aktualnie wybrany w panelu „Lata” (inaczej gra „znika” po imporcie, co wygląda jak błąd),
- decyzja, czy import ma od razu ustawiać `isClosed = true`. **Rekomendacja: nie** — admin zaznacza sam, gdy zweryfikuje dane. Jest to spójne z Q1(a) („zaznaczam grę, gdy jest rozliczona”).

### 14.5. Weryfikacja biblioteki do eksportu (Q9 = a) — sprawdzone praktycznie

Kandydat: **`xlsx-js-style`**, opis z npm: *„SheetJS Spreadsheet Community Edition plus Basic Cell Styles”*. Najnowsza wersja: **1.2.0**.

**Dostępność na CDN — uwaga, nie jest tam, gdzie się można spodziewać:**

| Adres | Wynik |
|---|---|
| `https://cdnjs.cloudflare.com/.../xlsx-js-style/1.2.0/xlsx.bundle.js` | **HTTP 404** — pakietu nie ma na cdnjs |
| `https://cdn.jsdelivr.net/npm/xlsx-js-style@1.2.0/dist/xlsx.bundle.js` | **HTTP 200**, 425 020 B |
| `https://unpkg.com/xlsx-js-style@1.2.0/dist/xlsx.bundle.js` | **HTTP 200**, 425 020 B |

W paczce **nie ma** pliku `xlsx.full.min.js` — dostępne są `dist/xlsx.bundle.js` i `dist/xlsx.min.js`. Oznacza to, że sam wzorzec adresu w `Main/index.html:989` musi się zmienić, nie tylko nazwa hosta.

**Test praktyczny (wykonany).** Zbudowano arkusz odpowiadający strukturze eksportu (nagłówki + wagi jako tekst + kwota jako liczba + procent jako tekst), ustawiono wyśrodkowanie, pogrubienie nagłówków, szerokości kolumn i format `# ##0`, zapisano plik i rozpakowano wynikowy XLSX. W wygenerowanym `xl/styles.xml` znalazło się:

```xml
<xf numFmtId="165" fontId="4" ... applyNumberFormat="1" applyAlignment="1">
  <alignment horizontal="center" vertical="center"/>
</xf>
...
<numFmts count="1"><numFmt numFmtId="165" formatCode="# ##0"/></numFmts>
```

a w `xl/worksheets/sheet1.xml` kolumny z `customWidth="1"` i komórki wskazujące na te style. **Wyśrodkowanie, separator tysięcy i szerokości kolumn działają — potwierdzone na wygenerowanym pliku, nie tylko w dokumentacji.**

**Ryzyka do przyjęcia świadomie:**
1. **Cofnięcie wersji silnika.** `xlsx-js-style@1.2.0` bazuje na SheetJS **0.18.5**, a aplikacja używa dziś **0.20.2**. To krok wstecz o dwie wersje minor. API wykorzystywane przez aplikację (`utils.aoa_to_sheet`, `utils.book_new`, `utils.book_append_sheet`, `writeFile`) jest w 0.18.5 obecne i zachowuje się identycznie — sprawdzone w teście. Globalna nazwa `XLSX` pozostaje ta sama, więc reszta kodu eksportu nie wymaga przepisania.
2. **Fork społecznościowy, nie oficjalny SheetJS.** Wolniejsze tempo aktualizacji niż upstream.
3. **Zmiana hosta CDN** z `cdn.sheetjs.com` na `cdn.jsdelivr.net`. Service Worker (`Main/service-worker.js`) przechwytuje **wyłącznie żądania same-origin** (warunek `isSameOrigin` w handlerze `fetch`), więc skrypt z CDN i tak nigdy nie był cache’owany — podmiana adresu **nie wymaga zmian w Service Workerze**.

**Efekt uboczny na plus:** biblioteka zapisuje w arkuszu `<ignoredErrors numberStoredAsText="1">`, więc Excel **nie pokaże** zielonych trójkącików „liczba zapisana jako tekst” przy kolumnach wag i procentów, które zgodnie z Q10 zostają tekstem.

### 14.6. Założenia przyjęte samodzielnie (drobne luki w odpowiedziach)

Poniższe kwestie nie były przedmiotem pytań, a wymagają rozstrzygnięcia. Przyjmuję rozwiązanie oczywiste, żeby praca mogła ruszyć — **każde z nich jest też zadane jako zwykłe pytanie prostym językiem w pkt 14.8** (pytania C–L). Jeśli któreś założenie nie odpowiada, wystarczy odpowiedzieć na to pytanie:

1. **Kolejność niepotwierdzonych w oknie „Status potwierdzeń”.** Q7(b) określa, że idą pod potwierdzonymi, ale nie mówi, w jakiej kolejności między sobą. Zakładam: **zachowują dotychczasową kolejność zapisania do gry**.
2. **Numeracja w adminowej „Gry do potwierdzenia”.** Zakładam **tę samą regułę** co w oknie „Status potwierdzeń” (Q6a + Q7b + Q8) — pierwotne zgłoszenie prosiło o tę funkcję w obu miejscach.
3. **Nagłówki w Excelu.** Zakładam **pogrubienie** nagłówków oprócz wyśrodkowania — mieści się w „dla lepszej widoczności” z pierwotnego zgłoszenia.
4. **Szerokości kolumn w Excelu.** Zakładam wyliczanie automatyczne z długości nagłówka i najdłuższej wartości, z rozsądnym minimum — zamiast jednej sztywnej szerokości dla wszystkich.
5. **Wagi w Excelu zostają tekstem** — analogicznie do decyzji o procentach (Q10). Separator tysięcy i tak nie ma dla nich znaczenia.
6. **Potwierdzenie przez admina za gracza** też nadaje numer w kolejce — ze znacznikiem czasu kliknięcia admina. Wynika wprost z modelu `confirmedAt`.

### 14.7. Plan realizacji (kolejność zatwierdzona w Q13)

> **STATUS: NIEAKTUALNY.** Obowiązuje plan z **pkt 15.6**, rozszerzony o etap 4 (potwierdzenia dla gier admina).

**Etap 1 — gotowy do startu, zero pytań otwartych, nic nie psuje**
1. Zgłoszenie 3 — naprawa przycisków „Waga1–Waga6” w zakładce „Statystyki”.
2. Zgłoszenie 4 — nazwy graczy rozwiązywane na żywo (Q11).
3. Punkt 10.2 — utrata fokusu przy ręcznej edycji wag.

**Etap 2 — gotowy do startu, decyzje podjęte**
4. Zgłoszenie 1 — „CzyZamknięta” filtruje statystyki i ranking (Q1a).
5. Zgłoszenie 6 — numeracja kolejności potwierdzeń (Q6a, Q7b, Q8).
6. Zgłoszenie 5 — eksport XLSX: podmiana biblioteki, wyśrodkowanie, szerokości, separator (Q9a, Q10).
7. Punkty 10.3, 10.4, 10.8 — escapowanie nazw, identyfikatory dokumentów, martwy kod (Q12).

**Etap 3 — zablokowany do czasu odpowiedzi**
8. Zgłoszenie 2 — „Importuj” (czeka na pytanie **A** z pkt 14.8).
9. Punkt 10.1 — licznik potwierdzeń w „Gry admina” (czeka na pytanie **B**; sensownie rozstrzygalny dopiero razem z importem).

**Poza planem, do osobnej decyzji później:** 10.5 (refaktor duplikacji), 10.6, 10.7, jednorazowe uzupełnienie `playerId` w starych wierszach.


### 14.8. Pytania pozostałe — prostym językiem

> **STATUS: wszystkie 12 pytań (A–L) ma odpowiedź** — plik `Analizy/pop3.docx`, zestawienie w **pkt 15.2**. Nowe pytania (M–Q) znajdują się w **pkt 15.7**.

Pytania podzieliłem na dwie grupy. **Grupa 1 blokuje pracę** — bez odpowiedzi nie da się zrobić przycisku „Importuj”. **Grupa 2 nie blokuje niczego** — na każde z tych pytań mam sensowną odpowiedź domyślną i jeśli Pan nic nie odpowie, zrobię tak, jak napisałem przy „Domyślnie”. Proszę zaznaczyć tylko te, gdzie chce Pan inaczej.

---

#### GRUPA 1 — te dwa pytania blokują pracę nad „Importuj”

**Pytanie A — zaimportowana gra: ma być „lustrem” gry gracza czy osobną kopią?**

Tu jest problem, bo Pana dwie odpowiedzi mówią co innego. W pytaniu 3 zaznaczył Pan, że gra ma być **kopią, która żyje własnym życiem**. W pytaniu 4 napisał Pan, że kopia ma się **sama aktualizować**. To się nie może zdarzyć naraz — albo coś jest kopią i można to swobodnie zmieniać, albo jest odbiciem oryginału i wtedy własne zmiany są kasowane przy każdej aktualizacji.

Dodatkowo: gdyby kopia w pełni się aktualizowała, to **odznaczałaby się jej „ptaszka” CzyZamknięta** przy każdej aktualizacji (bo u gracza gra jest otwarta), a wtedy gra wypadałaby ze statystyk. Czyli sama automatyczna aktualizacja zepsułaby to, co ustaliliśmy w pytaniu 1.

Proszę wybrać:
- **(a) Lustro, które można odpiąć.** Kopia sama nadąża za grą gracza (skład graczy, wpisowe, wypłaty, potwierdzenia). Dopóki jest podpięta, **nie da się jej ręcznie poprawiać** — poprawki robi się w grze gracza. Pana własne rzeczy — ptaszek „CzyZamknięta”, notatki po grze, wagi — **nigdy nie są nadpisywane**. Gdy uzna Pan, że gra jest już gotowa, klika **„Odłącz”** i od tej chwili to zwykła gra admina, w pełni edytowalna.
- **(b) Zwykła kopia + przycisk „Aktualizuj z gry gracza”.** Kopia od razu w pełni edytowalna, tak jak każda inna gra admina. Świeżą wersję od gracza pobiera Pan wtedy, kiedy sam kliknie. Nie aktualizuje się sama, ale za to nic nigdy nie nadpisze Pana poprawek.

*Moja rekomendacja: **(b)**. Jest prostsza, bezpieczniejsza i nie da się przez nią stracić ręcznej pracy. „Samo się aktualizuje” brzmi wygodnie, ale w praktyce oznacza, że aplikacja może Panu skasować poprawkę, której akurat nie patrzył Pan na ekranie.*

**Pytanie B — co zrobić z licznikiem potwierdzeń w zakładce „Gry admina”?**

Dziś ten licznik przy grach tworzonych ręcznie w „Gry admina” zawsze pokaże `0/8`, `0/9` itd., bo w aplikacji nie ma ekranu, na którym ktokolwiek mógłby taką grę potwierdzić. Po dodaniu „Importuj” zrobi się mieszanka: gry zaimportowane będą miały prawdziwe liczby (bo przyjdą razem z potwierdzeniami od gracza), a gry utworzone ręcznie dalej zero.

Proszę wybrać:
- **(a) Zostawić jak jest.** Przy grach zaimportowanych licznik pokazuje prawdę, przy ręcznie utworzonych — zero (bo faktycznie nikt ich nie potwierdza).
- **(b) Schować licznik i przycisk „Statusy”** przy grach utworzonych ręcznie, zostawić tylko przy zaimportowanych.
- **(c) Dorobić potwierdzanie także dla gier admina** — żeby gracze mogli potwierdzać udział również w nich. Najwięcej pracy z tych trzech.

*Moja rekomendacja: **(b)**. Pokazywanie licznika, który z zasady nie może pokazać nic innego niż zero, tylko wprowadza w błąd.*

---

#### GRUPA 2 — drobiazgi, praca ruszy bez odpowiedzi

**Pytanie C — w oknie „Status potwierdzeń” niepotwierdzeni idą na dół. Ale w jakiej kolejności między sobą?**
Ustaliliśmy, że potwierdzeni są na górze, w kolejności potwierdzania. Nie ustaliliśmy, jak poukładać tych, którzy jeszcze nie potwierdzili.
*Domyślnie: zostają w kolejności, w jakiej byli dopisywani do gry (czyli tak jak dziś).*

**Pytanie D — czy numerki mają być też w zakładce „Gry do potwierdzenia” u admina?**
W pierwotnym dokumencie pisał Pan „wprowadzić taką funkcję również w zakładce gry do potwierdzenia”, więc zakładam, że tak — i że mają działać dokładnie tak samo jak w oknie „Statusy”.
*Domyślnie: tak, ta sama zasada w obu miejscach.*

**Pytanie E — czy nagłówki w Excelu mają być pogrubione?**
Prosił Pan o wyśrodkowanie „dla lepszej widoczności”. Pogrubienie nagłówków to naturalny dodatek, ale nie było o nim mowy.
*Domyślnie: tak, nagłówki na pogrubione.*

**Pytanie F — jak szerokie mają być kolumny w Excelu?**
Można dać wszystkim jedną szerokość albo policzyć każdą osobno, tak żeby zmieściła się i nazwa kolumny, i najdłuższa wartość.
*Domyślnie: liczone osobno dla każdej kolumny — wtedy nic się nie ucina, a wąskie kolumny nie zajmują niepotrzebnie miejsca.*

**Pytanie G — kolumny „Waga” w Excelu: tekst czy liczba?**
Przy procentach powiedział Pan „zostawić jak jest”, czyli jako tekst. O wagach nie pytałem. Wagi to małe liczby (1, 5), więc separator tysięcy i tak nic tu nie zmienia. Różnica jest praktyczna: jeśli zostaną tekstem, Excel nie będzie umiał ich zsumować ani posortować po wielkości.
*Domyślnie: zostają tekstem, tak samo jak procenty.*

**Pytanie H — czy „Importuj” ma od razu zaznaczać ptaszek „CzyZamknięta”?**
Skoro tylko zaznaczone gry liczą się do statystyk, to pytanie brzmi: czy zaimportowana gra ma od razu wskoczyć do statystyk, czy poczekać, aż Pan ją sprawdzi i zaznaczy sam.
*Domyślnie: nie zaznacza — zaznacza Pan ręcznie, gdy zweryfikuje dane. Pasuje do tego, co napisał Pan w pytaniu 1: „zaznaczam grę, gdy jest rozliczona”.*

**Pytanie I — czy potwierdzenie wpisane przez Pana za gracza ma dostawać numerek w kolejce?**
Gdy gracz zgłosi się telefonicznie i Pan kliknie za niego „Potwierdź”, dostanie numer z chwili Pana kliknięcia — a nie z chwili, gdy naprawdę zadzwonił.
*Domyślnie: tak, dostaje numer normalnie, z chwili kliknięcia.*

**Pytanie J — co ze starymi wpisami, które nie mają ukrytego numeru gracza (ID)?**
Poprawka nazw graczy działa dzięki temu, że każdy gracz ma ukryty numer. Najstarsze wpisy w grach mogą go nie mieć — w takich wierszach nazwa zostanie stara, nawet po poprawce. Można to jednorazowo uzupełnić osobnym przebiegiem po bazie.
*Domyślnie: nie ruszam tego teraz. Najpierw zróbmy poprawkę, zobaczymy ile takich wierszy w ogóle zostało, a dopiero potem zdecydujemy, czy warto.*

**Pytanie K — czy przy okazji posprzątać w kodzie rzeczy, które niczego nie psują?**
Znalazłem trzy takie: ta sama logika liczenia statystyk jest napisana dwa razy (przez to właśnie powstał błąd z przyciskami „Waga”), dwie zakładki mogą sobie nawzajem nadpisać wagi przy jednoczesnej edycji, oraz statystyki przeliczają się kilkanaście razy przy wchodzeniu na stronę. Nic z tego nie przeszkadza w codziennej pracy.
*Domyślnie: **nie teraz.** Sprzątanie razem ze zmianami działania to prosta droga do tego, żeby nie wiedzieć, co zepsuło co. Proponuję najpierw wdrożyć i sprawdzić uzgodnione zmiany, a sprzątanie zrobić osobno, na spokojnie.*

**Pytanie L — czy po każdej zmianie wymuszać u wszystkich pobranie nowej wersji aplikacji?**
Aplikacja zapamiętuje swoje pliki w telefonie i komputerze, żeby działać szybciej. Po zmianach trzeba jej powiedzieć „to jest nowa wersja”, inaczej część osób przez jakiś czas widziałaby starą. Kosztem jest jednorazowe dłuższe wczytanie po aktualizacji.
*Domyślnie: tak, przy każdej wypuszczonej zmianie podbijam numer wersji — to standardowa praktyka i zabezpiecza przed sytuacją, w której Pan widzi poprawkę, a gracze nie.*

---

## 15. Odpowiedzi na pytania A–L i analiza nowej opcji importu (aktualizacja 2026-09-07, `pop3.docx`)

### 15.1. Prompt użytkownika (zachowany dla kontekstu)

> Wrzuciłem kolejny plik - Analizy/pop3.docx
> Zawiera on odpowiedzi na pytania oraz prośbę o poszerzenie analizy o jedną opcją. Rozbuduj analizę Analizy/Uwagi_2026-09-03.md - jeżeli masz jakieś dodatkowe pytania to przygotuj je jak poprzednio.

### 15.2. Odpowiedzi na pytania A–L

| # | Pytanie | Odpowiedź | Uwaga |
|---|---|---|---|
| **A** | Import: lustro czy osobna kopia? | **(b)** zwykła kopia + przycisk „Aktualizuj z gry gracza” | **plus prośba o analizę nowej opcji (C)** — pkt 15.3 |
| **B** | Licznik potwierdzeń w „Gry admina” | **(c)** dorobić potwierdzanie także dla gier admina | wbrew mojej rekomendacji (b) — konsekwencje w pkt 15.4 |
| **C** | Kolejność niepotwierdzonych | domyślna — kolejność zapisania do gry | |
| **D** | Numeracja też w „Gry do potwierdzenia” | domyślna — tak, ta sama zasada | |
| **E** | Pogrubione nagłówki w Excelu | domyślna — tak | |
| **F** | Szerokości kolumn w Excelu | domyślna — liczone osobno dla każdej kolumny | |
| **G** | Wagi w Excelu: tekst czy liczba? | „Tutaj nic nie zmieniamy zostaje jak było od samego początku” | patrz interpretacja niżej |
| **H** | Czy import zaznacza „CzyZamknięta”? | „Odpowiedź jest zawarta pod pytaniem A” | rozstrzygnięte przez opcję C — **nie zaznacza**, zaznacza admin |
| **I** | Numer przy potwierdzeniu przez admina | domyślna — tak, z chwili kliknięcia | |
| **J** | Stare wiersze bez `playerId` | domyślna — nie ruszamy teraz | |
| **K** | Sprzątanie w kodzie | domyślna — nie teraz | |
| **L** | Podbijanie wersji PWA | domyślna — tak, przy każdej zmianie | |

**Interpretacja odpowiedzi G.** „Zostaje jak było od samego początku” czytam jako: **kolumny wag nadal trafiają do Excela jako tekst**, bez zmiany typu — czyli dokładnie tak, jak działa to dziś i jak brzmiała odpowiedź domyślna. Wyśrodkowanie i szerokości kolumn obejmą je normalnie (to działa na komórkach tekstowych tak samo), separator tysięcy ich nie dotyczy, bo to liczby jednocyfrowe. Gdyby chodziło o coś innego — np. żeby kolumny wag w ogóle nie były formatowane — proszę o korektę, ale takie odczytanie nie miałoby praktycznego sensu wizualnego.

---

### 15.3. Nowa opcja (C): import automatyczny w chwili zamknięcia gry przez gracza

#### Propozycja użytkownika (cytat)

> Użytkownik tworzy grę, wprowadza dane. W momencie kiedy gra jest ukończona i wciśnie przycisk w kolumnie „czy zamknięta” gra importuje się do zakładki „gry admina”. W dalszym kroku po przeanalizowaniu i zamknięciu gry przez admina, dopiero taka gra wlicza się do statystyk. Uwagi: jeżeli gracz skasuje grę na swoim koncie, ta gra nie może być już skasowana z konta admina zakładka „gry admina”
> Czy to jest dobre rozwiązanie?

#### Odpowiedź krótka

**Tak — to jest lepsze rozwiązanie niż oba warianty, które proponowałem.** Wymaga trzech uzupełnień, opisanych niżej. Poniżej pełne uzasadnienie.

#### Dlaczego to jest dobre

**1. Rozwiązuje sprzeczność, przez którą utknęliśmy.**

Sprzeczność między pytaniem 3 a 4 („kopia żyje własnym życiem” kontra „kopia sama się aktualizuje”) brała się z jednego założenia: że import może nastąpić w dowolnym momencie, także gdy gra jest jeszcze w toku. Wtedy kopia rzeczywiście może się zdezaktualizować i trzeba wybierać między świeżością a niezależnością.

Nowa propozycja usuwa problem u źródła: **import następuje dokładnie raz, w momencie gdy gra jest skończona.** Nie ma czego synchronizować, bo źródło już się nie zmienia. Kopia może więc bez konfliktu być w pełni niezależna — czyli dokładnie to, co wybrał Pan w pytaniu 3 i w pytaniu A.

**2. Moment wyzwolenia jest naturalny i niczego nie trzeba pamiętać.**

W obu moich wariantach ktoś musiał pamiętać o kliknięciu „Importuj”. Tutaj sygnał daje osoba, która jako jedyna wie na pewno, że gra jest skończona — sam gracz. Admin nie musi pilnować listy cudzych gier.

**3. Powstaje czytelna dwustopniowa bramka.**

```
gracz wypełnia grę
      ↓ gracz zaznacza „CzyZamknięta”     ← „skończyłem, przekazuję”
kopia trafia do „Gry admina” (otwarta)
      ↓ admin sprawdza dane
      ↓ admin zaznacza „CzyZamknięta”     ← „sprawdzone, zaliczam”
gra wchodzi do statystyk i rankingu
```

Ten sam checkbox znaczy co innego po każdej stronie i w obu przypadkach znaczy to, co powinien: u gracza „skończone”, u admina „rozliczone”. Jest to w pełni spójne z odpowiedzią na pytanie 1 („zaznaczam grę, gdy jest rozliczona”). **To rozstrzyga też pytanie H** — import nie zaznacza checkboxa na kopii, robi to admin po weryfikacji.

**4. Warunek o kasowaniu jest spełniony automatycznie.**

„Jeżeli gracz skasuje grę na swoim koncie, ta gra nie może być już skasowana z konta admina” — kopia to osobne dokumenty w osobnej kolekcji (`Tables` kontra `UserGames`). Obecna procedura usuwania gry gracza (`Main/app.js:8000-8010` i odpowiednik w `initUserGamesManager`) kasuje wyłącznie dokument gry oraz jej podkolekcje `rows` i `confirmations` w **tej samej** kolekcji. Kopii u admina fizycznie nie dotyka. Warunek jest więc spełniony z samej konstrukcji — nie trzeba pisać na to żadnego zabezpieczenia.

#### Czego brakuje — trzy uzupełnienia, bez których to nie zadziała

**Uzupełnienie 1 — mechanizm nadrabiania zaległości (najważniejsze).**

Aplikacja nie ma serwera. Import wykona więc **przeglądarka gracza** w momencie kliknięcia. Jeżeli w tej chwili gracz jest offline, straci zasięg albo zamknie kartę zanim zapis dojdzie — gra zostanie u niego zamknięta, ale **nigdy nie dotrze do admina**, i nikt się o tym nie dowie.

Dlatego przy wejściu do zakładki „Gry admina” aplikacja musi dodatkowo sprawdzić: czy są gry użytkowników oznaczone jako zamknięte, dla których nie ma jeszcze kopii? Jeśli tak — dociągnąć je wtedy. Dzięki temu kliknięcie gracza jest tylko *przyspieszeniem*, a nie jedyną szansą na import. Bez tego mechanizmu rozwiązanie jest kruche.

**Uzupełnienie 2 — zabezpieczenie przed powtórnym importem.**

Gracz może odznaczyć i ponownie zaznaczyć „CzyZamknięta”. Bez zabezpieczenia powstałaby druga kopia i statystyki policzyłyby tę samą grę dwa razy.

Rozwiązanie: kopia dostaje pole `importedFromUserGameId` wskazujące grę źródłową; przed importem aplikacja sprawdza, czy kopia dla tego identyfikatora już istnieje. Świadomie **nie** proponuję trzymania znacznika wyłącznie po stronie gry gracza — gdyby admin usunął kopię, znacznik zablokowałby ponowny import i gry nie dałoby się już odzyskać.

**Uzupełnienie 3 — co przy powtórnym zamknięciu z poprawionymi danymi.**

Scenariusz realny: gracz zamyka grę, po godzinie zauważa błąd w wypłacie, odznacza, poprawia, zaznacza ponownie. Kopia u admina ma już stare liczby.

Tu wchodzi Pana odpowiedź na pytanie A: **przycisk „Aktualizuj z gry gracza”** na kopii u admina. Automat nie nadpisuje niczego sam (więc ręczne poprawki admina są bezpieczne), a admin jednym kliknięciem pobiera świeżą wersję, gdy chce.

Innymi słowy: **opcja (b) i opcja (C) nie są alternatywami — one się uzupełniają.** (C) daje automatyczne wyzwolenie w dobrym momencie, (b) daje kontrolowaną aktualizację, gdyby źródło się jeszcze zmieniło.

#### Słabsze punkty, o których trzeba wiedzieć

1. **Przeglądarka gracza zapisuje do danych admina.** Reguły Firestore na to pozwalają (`Analizy/Wazne_Rules.txt`: `match /Tables/{tableId} { allow read, write: if true; }`). Warto jednak wiedzieć, że **całe reguły bazy są dziś otwarte dla wszystkich** — każdy, kto zna adres projektu, może pisać do dowolnej kolekcji. Import niczego tu nie pogarsza, ale to dobry moment, żeby o tym powiedzieć wprost — temat wydzielony do osobnej analizy `Analizy/Bezpieczenstwo_Firestore_2026-09-07.md`.
2. **Checkbox u gracza zyskuje drugie znaczenie.** Do tej pory zaznaczenie „CzyZamknięta” tylko chowało grę z listy potwierdzeń. Teraz dodatkowo wysyła ją do admina. Przypadkowe kliknięcie wyśle grę przed czasem — dlatego proponuję krótkie pytanie potwierdzające (pytanie **N**).
3. **Kolizja z wyborem B=(c)** — opisana w pkt 15.4, wymaga jednego dodatkowego filtra.
4. **Co z grami, których gracz nigdy nie zamknie?** Gra rozegrana, ale niezamknięta przez gracza, nigdy nie trafi do admina. Dlatego proponuję zostawić także ręczny przycisk „Importuj” z pierwotnego zgłoszenia (pytanie **M**).

#### Werdykt

Rozwiązanie oceniam jako **dobre i lepsze od moich propozycji** — pod warunkiem dołożenia uzupełnienia 1 (nadrabianie zaległości) i 2 (blokada duplikatu). Uzupełnienie 3 wynika już z Pana odpowiedzi na pytanie A.

Największą zaletą jest to, że **znika cała klasa problemów z synchronizacją** — a to była jedyna rzecz blokująca ten etap prac.

---

### 15.4. Skutki wyboru B = (c): potwierdzenia także dla gier admina

> **STATUS: ANULOWANE.** Odpowiedź **P** (`pop4.docx`) cofa wybór B=(c) — gry admina pozostają odseparowane od potwierdzeń. Cała ta sekcja jest nieaktualna, w tym opisana w niej kolizja, która wraz z anulowaniem przestaje istnieć. Szczegóły w **pkt 16.5**.

Wybrał Pan najszerszy wariant — gracze mają móc potwierdzać udział również w grach tworzonych bezpośrednio w „Gry admina”. To rozwiązanie sensowne (admin planuje grę, gracze się zapisują), ale ma trzy konsekwencje, które trzeba obsłużyć.

#### Co trzeba zmienić

- `getActiveGamesForConfirmationsFromCollections` wywoływane jest dziś z listą jednoelementową `[UserGames]` w dwóch miejscach: `Main/app.js:2381` (widok gracza) i `Main/app.js:3771` (widok admina) — trzeba dodać kolekcję gier admina,
- `nextGamesState` ma dziś gry admina zahardkodowane jako pustą tablicę (`Main/app.js:1908`, `2020-2021`) — trzeba przywrócić ich agregację, żeby „Najbliższa gra” obejmowała też gry admina,
- funkcja zapisu potwierdzenia przyjmuje już `collectionName` z rekordu gry, więc sama obsługa `Potwierdź` / `Anuluj` zadziała dla gier admina bez zmian,
- licznik `IlośćPotwierdzonych` i przycisk `Statusy` w „Gry admina” zaczną wtedy pokazywać prawdziwe dane — czyli pierwotny problem z pkt 10.1 znika sam.

#### Konsekwencja 1 — zaimportowane gry trafiłyby na listę „do potwierdzenia”

To jest realna kolizja z opcją (C) i trzeba ją obsłużyć.

Świeżo zaimportowana kopia jest **otwarta** (`isClosed = false`, bo zamyka ją dopiero admin po weryfikacji — pkt 15.3). Lista „Gry do potwierdzenia” filtruje wyłącznie po `isClosed`, **bez filtra daty**. Efekt: gra już rozegrana i przysłana przez gracza pojawiłaby się graczom jako „do potwierdzenia” — dopóki admin jej nie zamknie.

Widok „Najbliższa gra” jest na to w większości odporny, bo dodatkowo filtruje po dacie (`isTodayOrFutureGame`, `Main/app.js:1935`) — ale gra rozegrana i zamknięta **tego samego dnia** przeszłaby i przez ten filtr.

Rozwiązanie: wykluczyć z obu list gry mające ustawione `importedFromUserGameId`. Zaimportowana kopia jest z definicji grą już rozegraną, więc nie ma czego w niej potwierdzać.

#### Konsekwencja 2 — cofamy wcześniejszą świadomą decyzję

W `Main/docs/Documentation.md` istnieje sekcja „Aktualizacja techniczna: separacja Gry admina od potwierdzeń i Najbliższej gry”, opisująca odcięcie gier admina od obu tych obiegów. Sprawdziłem historię repozytorium (commit `5913ae8` z 2026-04-20) — **powód tej decyzji nie został nigdzie zapisany**, dokumentacja odnotowuje tylko, co zrobiono.

Wybór B=(c) tę decyzję odwraca. Jeśli separacja była reakcją na jakiś konkretny problem (np. gry admina zaśmiecały listę potwierdzeń graczom), problem wróci. Stąd pytanie **P** w pkt 15.7.

#### Konsekwencja 3 — potwierdzenia kopiowane przy imporcie

Skoro potwierdzenia zaczną działać dla gier admina, to import powinien przenieść także podkolekcję `confirmations` — inaczej licznik przy zaimportowanej grze pokazywałby zero mimo że gracze potwierdzali udział u siebie. Numeracja kolejności (`confirmedAt`) przeniesie się razem z nimi, więc historia „kto był pierwszy” zostanie zachowana.

---

### 15.5. Zaktualizowany cykl życia gry

> **STATUS: NIEAKTUALNE.** Obowiązuje cykl z **pkt 16.6** (ścieżka 2 nie zawiera już kroku z potwierdzeniami).

Po wszystkich ustaleniach istnieją **dwie ścieżki**, obie kończące się tym samym warunkiem wejścia do statystyk.

**Ścieżka 1 — gra założona przez gracza**

| Krok | Kto | Co się dzieje |
|---|---|---|
| 1 | gracz | tworzy grę w „Gry użytkowników”, wpisuje skład i kwoty |
| 2 | gracze | potwierdzają udział (numeracja kolejności wg `confirmedAt`) |
| 3 | gracz | zaznacza „CzyZamknięta” → **automatyczny import kopii do „Gry admina”** |
| 4 | — | kopia powstaje jako **otwarta**, z `importedFromUserGameId`; wykluczona z list potwierdzeń |
| 5 | admin | weryfikuje dane; w razie potrzeby klika „Aktualizuj z gry gracza” |
| 6 | admin | zaznacza „CzyZamknięta” na kopii → **gra wchodzi do statystyk i rankingu** |

**Ścieżka 2 — gra założona przez admina**

| Krok | Kto | Co się dzieje |
|---|---|---|
| 1 | admin | tworzy grę bezpośrednio w „Gry admina” |
| 2 | gracze | potwierdzają udział — **nowość z B=(c)**; gra widoczna w „Gry do potwierdzenia” i „Najbliższa gra” |
| 3 | admin | uzupełnia wyniki po rozegraniu |
| 4 | admin | zaznacza „CzyZamknięta” → **gra wchodzi do statystyk i rankingu** |

Gra gracza zostaje u niego na koncie w obu przypadkach i nigdy nie jest kasowana automatycznie.

---

### 15.6. Zaktualizowany plan realizacji

> **STATUS: NIEAKTUALNE.** Obowiązuje plan z **pkt 16.8**.

**Etap 1 — gotowy, zero pytań otwartych**
1. Naprawa przycisków „Waga1–Waga6” w zakładce „Statystyki”.
2. Nazwy graczy rozwiązywane na żywo.
3. Utrata fokusu przy ręcznej edycji wag (pkt 10.2).

**Etap 2 — gotowy, wszystkie decyzje podjęte**
4. „CzyZamknięta” filtruje statystyki i ranking.
5. Numeracja kolejności potwierdzeń (C, D, I — potwierdzone).
6. Eksport XLSX: podmiana biblioteki, wyśrodkowanie, pogrubione nagłówki, szerokości per kolumna, separator tysięcy; procenty i wagi zostają tekstem (E, F, G — potwierdzone).
7. Escapowanie nazw graczy, identyfikatory dokumentów, martwy kod (pkt 10.3, 10.4, 10.8).

**Etap 3 — import (odblokowany co do modelu, czeka na drobiazgi M, N, O)**
8. Automatyczny import przy zamknięciu gry przez gracza + nadrabianie zaległości + blokada duplikatu.
9. Przycisk „Aktualizuj z gry gracza” na kopii u admina.
10. Wykluczenie zaimportowanych kopii z list potwierdzeń.

**Etap 4 — potwierdzenia dla gier admina (B = c, czeka na pytanie P)**
11. Włączenie gier admina do „Gry do potwierdzenia” i „Najbliższa gra”.
12. Licznik `IlośćPotwierdzonych` w „Gry admina” zaczyna działać (pkt 10.1 rozwiązany).

**Poza planem:** pkt 10.5, 10.6, 10.7 oraz uzupełnianie `playerId` w starych wierszach (K, J — odłożone).

---

### 15.7. Nowe pytania — prostym językiem

> **STATUS: pytania M–P mają odpowiedź** (`pop4.docx`) — zestawienie w **pkt 16.2**. Nowe pytania R i S są w **pkt 16.7**.

Żadne z tych pytań nie blokuje Etapu 1 ani Etapu 2 — te mogę robić od zaraz. Dotyczą wyłącznie importu i potwierdzeń dla gier admina. Przy każdym jest odpowiedź domyślna: jeśli Pan nic nie odpowie, zrobię tak, jak napisałem.

**Pytanie M — czy oprócz automatu zostaje też ręczny przycisk „Importuj”?**
W pierwotnym dokumencie prosił Pan o przycisk „Importuj” w każdym wierszu gry użytkownika. Teraz import ma się dziać sam, gdy gracz zamknie grę. Pytanie, czy przycisk nadal jest potrzebny — przydałby się wtedy, gdy gracz rozegrał grę, ale zapomniał ją zamknąć, a Pan chce ją mimo to policzyć.
*Domyślnie: zostawiam przycisk. Nic nie kosztuje, a ratuje sytuacje, w których gracz o czymś zapomniał.*

**Pytanie N — czy gracz ma dostać pytanie „na pewno?” przy zamykaniu gry?**
Po zmianie zaznaczenie „CzyZamknięta” u gracza wysyła grę do Pana. Przypadkowe kliknięcie wyśle ją przed czasem.
*Domyślnie: tak, krótkie pytanie w stylu „Zamknąć grę i przekazać ją administratorowi?”. Gracz może odznaczyć checkbox z powrotem, ale kopia u Pana już zostanie — więc lepiej zapytać raz.*

**Pytanie O — co ma się stać, gdy Pan skasuje kopię w „Gry admina”?**
Kopia jest niezależna, więc jej skasowanie nie rusza gry gracza. Pytanie brzmi, czy gra może wrócić.
- **(a)** Może wrócić — gdy gracz ponownie zaznaczy „CzyZamknięta” (albo Pan kliknie „Importuj”), kopia utworzy się na nowo.
- **(b)** Nie wraca — skasowanie oznacza „ta gra mnie nie interesuje” i aplikacja pamięta, żeby jej już nie przysyłać.
*Domyślnie: **(a)**. Skasowanie kopii to zwykle pomyłka albo chęć zaimportowania od nowa, a wariant (b) tworzy sytuację bez wyjścia — gry nie dałoby się już odzyskać inaczej niż ręcznym przepisaniem.*

**Pytanie P — czy separacja gier admina od potwierdzeń miała jakiś powód?**
Wybrał Pan wariant (c), czyli gracze będą mogli potwierdzać udział także w grach tworzonych przez Pana. Kiedyś zostało to celowo rozdzielone — w dokumentacji jest o tym notatka, ale **nie ma zapisanego powodu**, a historia repozytorium to zbiorcze wgrania, więc nie da się go odtworzyć.
*Pytanie: czy pamięta Pan, dlaczego to rozdzielono? Jeśli chodziło o konkretny problem — np. że gry admina niepotrzebnie zaśmiecały graczom listę „Gry do potwierdzenia” — to ten problem wróci i lepiej go od razu obejść.*
*Domyślnie: robię tak, jak Pan wybrał (wariant c), i dodatkowo wykluczam z listy gry zaimportowane, żeby nie pojawiały się tam gry już rozegrane.*

> **Pytanie Q (reguły dostępu do bazy danych) zostało wyjęte z tej analizy** na prośbę użytkownika i jest przedmiotem osobnego opracowania: `Analizy/Bezpieczenstwo_Firestore_2026-09-07.md`.

---

## 16. Odpowiedzi na pytania M–P (aktualizacja 2026-09-07, `pop4.docx`)

### 16.1. Prompt użytkownika (zachowany dla kontekstu)

> Do kwestii bezpieczeństwa wrócimy potem w innej rozmowie. Tymczasem wgrałem nowy plik Analizy/pop4.docx z odpowiedziami na pytania. Zaktualizuj Analizy/Bezpieczenstwo_Firestore_2026-09-07.md - dopisz do niego też, że ostatnim etapem poprawek ma być kontynuacja problemu z naprawą bezpieczeństwa opisanego w Analizy/Bezpieczenstwo_Firestore_2026-09-07.md - czy mamy już wszystko gotowe i uzgodnione, żeby wykonać pierwszy etap (czyli wszystko do bezpieczeństwa firestore)?

Zakres pliku `pop4.docx`: odpowiedzi na pytania **M, N, O, P** z pkt 15.7. Plik **nie zawiera** odpowiedzi na pytania 1–7 z analizy bezpieczeństwa (`Bezpieczenstwo_Firestore_2026-09-07.md`, pkt 12.7) — i nie musi, bo bezpieczeństwo zostało przesunięte na ostatni etap.

### 16.2. Odpowiedzi

| # | Pytanie | Odpowiedź |
|---|---|---|
| **M** | Czy zostaje ręczny przycisk „Importuj”? | **NIE.** „Przycisk »importuj« nie jest już chyba konieczny. jeśli gracz zapomni zakończyć grę, mogę zakończyć ją samodzielnie z pozycji administratora.” |
| **N** | Pytanie „na pewno?” przy zamykaniu gry? | **NIE.** Dodatkowo: przy ponownym zamknięciu z nowymi danymi ma nastąpić **aktualizacja tej samej gry** w „Gry admina”, a nie utworzenie drugiej kopii |
| **O** | Czy skasowana kopia może wrócić? | **(a) tak** — ponowne zamknięcie gry tworzy ją na nowo |
| **P** | Separacja gier admina od potwierdzeń | **Zostaje separacja.** „Tak już pamiętam, jednak rozdzielamy gry admina od potwierdzeń. Zostawiamy tak jak było wcześniej.” |

### 16.3. Skutki odpowiedzi M — znika pierwotne zgłoszenie nr 2

Rezygnacja z przycisku „Importuj” **uchyla pierwotne zgłoszenie nr 2** z `apka pop.docx` (dodatkowa kolumna „Importuj” za przyciskiem „Usuń”). Nie będzie nowej kolumny w tabeli „Gry użytkowników”.

Rozwiązanie zastępcze, które Pan wskazał, **działa już dziś bez żadnych zmian**: administrator ma zakładkę „Gry użytkowników” z tym samym checkboxem `CzyZamknięta`, więc może zamknąć cudzą grę samodzielnie.

**Ważna konsekwencja techniczna — i akurat korzystna.** Widok gracza i widok administratora dla gier użytkowników to **ten sam kod** (`initUserGamesManager`, wywoływany przez `initPlayerUserGames` i `initAdminUserGames`), a checkbox obsługiwany jest w jednym miejscu (`Main/app.js:3293-3302`). Wystarczy więc podpiąć wyzwalanie importu w tym jednym miejscu, żeby zadziałało **niezależnie od tego, kto zaznaczy checkbox** — gracz u siebie czy administrator w swojej zakładce. Jedna implementacja pokrywa oba scenariusze.

### 16.4. Skutki odpowiedzi N — import aktualizuje istniejącą kopię

Rezygnuję z pytania potwierdzającego. Istotniejsza jest druga część odpowiedzi: **ponowne zamknięcie gry ma zaktualizować istniejącą kopię, a nie tworzyć drugą.**

Reguła do wdrożenia:

> Zaznaczenie `CzyZamknięta` na grze użytkownika (przez gracza albo administratora):
> - jeżeli kopii w „Gry admina” **nie ma** → utwórz ją,
> - jeżeli kopia **już istnieje** → odśwież w niej dane gry.

**Co jest odświeżane, a co nie.** To musi być rozstrzygnięte precyzyjnie, inaczej odświeżenie skasowałoby pracę administratora:

| Pole | Zachowanie przy odświeżeniu | Dlaczego |
|---|---|---|
| skład graczy (`rows`), kwoty, punkty, mistrzostwo | **nadpisywane** ze źródła | to jest sens „aktualizacji tej samej gry” |
| nazwa, data, rodzaj gry | **nadpisywane** ze źródła | jw. |
| potwierdzenia (`confirmations`) | **nadpisywane** ze źródła | zachowuje historię „kto potwierdził i w jakiej kolejności” |
| **`CzyZamknięta` na kopii** | **nietykane** | to jest decyzja administratora o zaliczeniu gry do statystyk (odpowiedź Q1a); nadpisanie cofałoby weryfikację |
| **`Notatki po grze` na kopii** | **nietykane** | to własna notatka administratora, nie ma jej w grze gracza |
| wagi i wpisy w `admin_games_stats` | **nietykane** | dane roczne, nie należą do pojedynczej gry |

**Skutek uboczny, o którym trzeba wiedzieć:** jeżeli administrator ręcznie poprawi kwoty na kopii, a gracz później znów zamknie swoją grę — **poprawki administratora zostaną nadpisane**. Wynika to wprost z „aktualizacji tej samej gry” i nie da się tego pogodzić z jednoczesnym zachowaniem ręcznych zmian. Patrz pytanie **R** w pkt 16.7.

**Przycisk „Aktualizuj z gry gracza”** (odpowiedź na pytanie A) zostaje, ale zmienia rolę: automat obsługuje moment zamknięcia, a przycisk przydaje się wtedy, gdy gracz poprawi dane **bez** odznaczania i ponownego zaznaczania checkboxa — bo wtedy żaden automat się nie uruchomi.

### 16.5. Skutki odpowiedzi P — cofnięcie decyzji B=(c)

To jest najważniejsza zmiana w tej turze. Wcześniejszy wybór **B=(c)** („dorobić potwierdzanie także dla gier admina”) zostaje **wycofany**. Gry admina pozostają odseparowane od potwierdzeń, tak jak działa to dziś.

**Co z tego wypada z planu:**
- ❌ **Etap 4 w całości** — nie włączamy gier admina do „Gry do potwierdzenia” ani do „Najbliższa gra”,
- ❌ zmiany w `getActiveGamesForConfirmationsFromCollections` (`Main/app.js:2381`, `3771`) — niepotrzebne,
- ❌ przywracanie agregacji gier admina w `nextGamesState` (`Main/app.js:1908`, `2020-2021`) — niepotrzebne.

**Co z tego znika jako problem — i bardzo dobrze:**
- ✅ **Kolizja opisana w pkt 15.4 (konsekwencja 1) przestaje istnieć.** Obawa, że świeżo zaimportowana kopia (otwarta, bo zamyka ją dopiero admin) pojawi się graczom na liście „do potwierdzenia”, była skutkiem wyłącznie wyboru B=(c). Skoro gry admina nigdy nie trafiają do tego obiegu, nie trzeba żadnego dodatkowego filtra ani znacznika. **Mniej kodu i mniej ryzyka.**
- ✅ Nie cofamy wcześniejszej świadomej decyzji projektowej, co było moim zastrzeżeniem w pkt 15.4 (konsekwencja 2).

**Co wraca jako otwarta sprawa:**
- ⚠️ **Punkt 10.1** — licznik `IlośćPotwierdzonych` i przycisk `Statusy` w zakładce „Gry admina”. Miał zostać rozwiązany przez B=(c); skoro tego nie robimy, wraca do rozstrzygnięcia. Sytuacja jest jednak inna niż wcześniej, bo import przenosi potwierdzenia gracza — patrz pytanie **S** w pkt 16.7.

### 16.6. Zaktualizowany cykl życia gry

Ścieżka 2 (gra zakładana przez admina) upraszcza się po cofnięciu B=(c) — nie ma w niej kroku z potwierdzeniami.

**Ścieżka 1 — gra założona przez gracza**

| Krok | Kto | Co się dzieje |
|---|---|---|
| 1 | gracz | tworzy grę w „Gry użytkowników”, wpisuje skład i kwoty |
| 2 | gracze | potwierdzają udział (numeracja kolejności wg `confirmedAt`) |
| 3 | gracz **lub admin** | zaznacza `CzyZamknięta` na grze użytkownika → **kopia trafia do „Gry admina”** (nowa albo odświeżona) |
| 4 | — | kopia powstaje jako **otwarta**; nie uczestniczy w obiegu potwierdzeń, bo gry admina są z niego wyłączone |
| 5 | admin | weryfikuje dane; w razie potrzeby klika „Aktualizuj z gry gracza” |
| 6 | admin | zaznacza `CzyZamknięta` na kopii → **gra wchodzi do statystyk i rankingu** |

**Ścieżka 2 — gra założona przez admina**

| Krok | Kto | Co się dzieje |
|---|---|---|
| 1 | admin | tworzy grę bezpośrednio w „Gry admina” |
| 2 | admin | uzupełnia wyniki po rozegraniu |
| 3 | admin | zaznacza `CzyZamknięta` → **gra wchodzi do statystyk i rankingu** |

Potwierdzenia obecności działają **wyłącznie** w obiegu gier użytkowników — bez zmian względem stanu obecnego.

### 16.7. Nowe pytania — prostym językiem

Oba dotyczą wyłącznie importu i **mają odpowiedzi domyślne**, więc nie wstrzymują prac.

**Pytanie R — co, jeśli poprawi Pan kopię ręcznie, a gracz potem znowu zamknie swoją grę?**
Ustaliliśmy, że ponowne zamknięcie aktualizuje tę samą grę w „Gry admina”. Jeśli w międzyczasie poprawił Pan tam kwoty, ta aktualizacja je nadpisze.
- **(a)** Niech nadpisuje bez pytania — dane gracza są zawsze ważniejsze.
- **(b)** Niech aplikacja ostrzeże, że kopia była ręcznie zmieniana, i pozwoli zdecydować.
*Domyślnie: **(a)**, ale z widoczną datą ostatniego odświeżenia przy grze — żeby było wiadomo, że coś przyszło od gracza. Wariant (b) wymaga pamiętania, które pola były ruszane, co komplikuje kod bardziej, niż jest to warte przy tej skali.*

**Pytanie S — co z licznikiem potwierdzeń w zakładce „Gry admina”?**
Wcześniej wybrał Pan wariant (c) — potwierdzanie także dla gier admina — ale teraz wracamy do separacji, więc ten wybór odpada, a pytanie wraca. Nowa okoliczność: gry zaimportowane przyniosą ze sobą potwierdzenia od gracza, więc przy nich licznik pokaże prawdziwe liczby. Zero będzie tylko przy grach, które Pan sam założył w „Gry admina”.
- **(a)** Zostawić jak jest — przy zaimportowanych prawda, przy własnych zero.
- **(b)** Schować licznik i przycisk „Statusy” przy grach założonych ręcznie w „Gry admina”, zostawić tylko przy zaimportowanych.
*Domyślnie: **(b)**. Przy zaimportowanych grach licznik i lista „kto potwierdził i w jakiej kolejności” są cenne — to gotowy zapis historii. Przy grach zakładanych przez Pana licznik z definicji pokaże zero, więc lepiej go tam nie pokazywać, bo wygląda jak usterka.*

### 16.8. Zaktualizowany plan realizacji

Bezpieczeństwo Firestore zostaje przesunięte na **ostatni etap** (patrz `Analizy/Bezpieczenstwo_Firestore_2026-09-07.md`, pkt 13).

**Etap 1 — naprawy, zero pytań otwartych**
1. Przyciski „Waga1–Waga6” w zakładce „Statystyki” (`ReferenceError`).
2. Nazwy graczy rozwiązywane na żywo.
3. Utrata fokusu przy ręcznej edycji wag (pkt 10.2).

**Etap 2 — zmiany działania, wszystko uzgodnione**
4. `CzyZamknięta` filtruje statystyki i ranking (tylko zaznaczone gry).
5. Numeracja kolejności potwierdzeń — modal „Status potwierdzeń” i adminowa „Gry do potwierdzenia”.
6. Eksport XLSX — podmiana biblioteki na `xlsx-js-style`, wyśrodkowanie, pogrubione nagłówki, szerokości per kolumna, separator tysięcy.
7. Usterki dodatkowe: escapowanie nazw graczy (10.3), identyfikatory dokumentów (10.4), martwy kod (10.8).

**Etap 3 — import gier użytkowników**
8. Automatyczny import przy zaznaczeniu `CzyZamknięta` (gracz albo admin), z aktualizacją istniejącej kopii zamiast tworzenia duplikatu.
9. Nadrabianie zaległości przy wejściu do „Gry admina” (dla zamknięć, które nie dotarły).
10. Przycisk „Aktualizuj z gry gracza” na kopii.
11. Rozstrzygnięcie licznika `IlośćPotwierdzonych` w „Gry admina” (pytanie S).

**Etap 4 — bezpieczeństwo Firestore** *(ostatni)*
12. Kopia zapasowa i przywracanie, App Check — zgodnie z `Analizy/Bezpieczenstwo_Firestore_2026-09-07.md`.

**Anulowane:** dawny Etap 4 (potwierdzenia dla gier admina) — wycofany odpowiedzią P. **Poza planem:** pkt 10.5, 10.6, 10.7 oraz uzupełnianie `playerId` w starych wierszach.

### 16.9. Obowiązki dokumentacyjne przy realizacji

Zgodnie z `Main/AGENTS.md` każda zmiana pociąga aktualizację dokumentacji. Odnotowuję, żeby nie umknęło:

- `Main/docs/README.md` i `Main/docs/Documentation.md` — po każdej zmianie w kodzie,
- `Kolumny.md` — nowa kolumna z numeracją w modalu „Status potwierdzeń” i w adminowej „Gry do potwierdzenia”; **kolumna „Importuj” nie powstanie** (odpowiedź M),
- `DetaleLayout.md` — jeśli zmiany dotkną wyglądu,
- podbicie wersji PWA (`APP_VERSION` w `Main/service-worker.js` oraz `?v=` w `Main/index.html`) przy każdym wydaniu — odpowiedź L.
